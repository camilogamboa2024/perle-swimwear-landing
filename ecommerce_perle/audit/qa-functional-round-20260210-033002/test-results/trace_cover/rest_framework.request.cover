    1: """
       The Request class is used as a wrapper around the standard request object.
       
       The wrapped request then offers a richer API, in particular :
       
           - content automatically parsed according to `Content-Type` header,
             and available as `request.data`
           - full support of PUT method, including support for file uploads
           - form overloading of HTTP method, content type and content
       """
    1: import io
    1: import sys
    1: from contextlib import contextmanager
       
    1: from django.conf import settings
    1: from django.http import HttpRequest, QueryDict
    1: from django.http.request import RawPostDataException
    1: from django.utils.datastructures import MultiValueDict
    1: from django.utils.http import parse_header_parameters
       
    1: from rest_framework import exceptions
    1: from rest_framework.settings import api_settings
       
       
    1: def is_form_media_type(media_type):
           """
           Return True if the media type is a valid form media type.
           """
   32:     base_media_type, params = parse_header_parameters(media_type)
   64:     return (base_media_type == 'application/x-www-form-urlencoded' or
   32:             base_media_type == 'multipart/form-data')
       
       
    2: class override_method:
    1:     """
           A context manager that temporarily overrides the method on a request,
           additionally setting the `view.request` attribute.
       
           Usage:
       
               with override_method(view, request, 'POST') as request:
                   ... # Do stuff with `view` and `request`
           """
       
    1:     def __init__(self, view, request, method):
               self.view = view
               self.request = request
               self.method = method
               self.action = getattr(view, 'action', None)
       
    1:     def __enter__(self):
               self.view.request = clone_request(self.request, self.method)
               # For viewsets we also set the `.action` attribute.
               action_map = getattr(self.view, 'action_map', {})
               self.view.action = action_map.get(self.method.lower())
               return self.view.request
       
    1:     def __exit__(self, *args, **kwarg):
               self.view.request = self.request
               self.view.action = self.action
       
       
    2: class WrappedAttributeError(Exception):
    1:     pass
       
       
    2: @contextmanager
    2: def wrap_attributeerrors():
           """
           Used to re-raise AttributeErrors caught during authentication, preventing
           these errors from otherwise being handled by the attribute access protocol.
           """
   40:     try:
   40:         yield
    4:     except AttributeError:
               info = sys.exc_info()
               exc = WrappedAttributeError(str(info[1]))
               raise exc.with_traceback(info[2])
       
       
    2: class Empty:
    1:     """
           Placeholder for unset attributes.
           Cannot use `None`, as that may be a valid value.
           """
    1:     pass
       
       
    1: def _hasattr(obj, name):
   64:     return not getattr(obj, name) is Empty
       
       
    1: def clone_request(request, method):
           """
           Internal helper method to clone a request, replacing with a different
           HTTP method.  Used for checking permissions against other methods.
           """
           ret = Request(request=request._request,
                         parsers=request.parsers,
                         authenticators=request.authenticators,
                         negotiator=request.negotiator,
                         parser_context=request.parser_context)
           ret._data = request._data
           ret._files = request._files
           ret._full_data = request._full_data
           ret._content_type = request._content_type
           ret._stream = request._stream
           ret.method = method
           if hasattr(request, '_user'):
               ret._user = request._user
           if hasattr(request, '_auth'):
               ret._auth = request._auth
           if hasattr(request, '_authenticator'):
               ret._authenticator = request._authenticator
           if hasattr(request, 'accepted_renderer'):
               ret.accepted_renderer = request.accepted_renderer
           if hasattr(request, 'accepted_media_type'):
               ret.accepted_media_type = request.accepted_media_type
           if hasattr(request, 'version'):
               ret.version = request.version
           if hasattr(request, 'versioning_scheme'):
               ret.versioning_scheme = request.versioning_scheme
           return ret
       
       
    2: class ForcedAuthentication:
    1:     """
           This authentication class is used if the test client or request factory
           forcibly authenticated the request.
           """
       
    1:     def __init__(self, force_user, force_token):
               self.force_user = force_user
               self.force_token = force_token
       
    1:     def authenticate(self, request):
               return (self.force_user, self.force_token)
       
       
    2: class Request:
    1:     """
           Wrapper allowing to enhance a standard `HttpRequest` instance.
       
           Kwargs:
               - request(HttpRequest). The original request instance.
               - parsers(list/tuple). The parsers to use for parsing the
                 request content.
               - authenticators(list/tuple). The authenticators used to try
                 authenticating the request's user.
           """
       
    2:     def __init__(self, request, parsers=None, authenticators=None,
    1:                  negotiator=None, parser_context=None):
   23:         assert isinstance(request, HttpRequest), (
                   'The `request` argument must be an instance of '
                   '`django.http.HttpRequest`, not `{}.{}`.'
                   .format(request.__class__.__module__, request.__class__.__name__)
               )
       
   23:         self._request = request
   23:         self.parsers = parsers or ()
   23:         self.authenticators = authenticators or ()
   23:         self.negotiator = negotiator or self._default_negotiator()
   23:         self.parser_context = parser_context
   23:         self._data = Empty
   23:         self._files = Empty
   23:         self._full_data = Empty
   23:         self._content_type = Empty
   23:         self._stream = Empty
       
   23:         if self.parser_context is None:
                   self.parser_context = {}
   23:         self.parser_context['request'] = self
   23:         self.parser_context['encoding'] = request.encoding or settings.DEFAULT_CHARSET
       
   23:         force_user = getattr(request, '_force_auth_user', None)
   23:         force_token = getattr(request, '_force_auth_token', None)
   23:         if force_user is not None or force_token is not None:
                   forced_auth = ForcedAuthentication(force_user, force_token)
                   self.authenticators = (forced_auth,)
       
    1:     def __repr__(self):
               return '<%s.%s: %s %r>' % (
                   self.__class__.__module__,
                   self.__class__.__name__,
                   self.method,
                   self.get_full_path())
       
           # Allow generic typing checking for requests.
    1:     def __class_getitem__(cls, *args, **kwargs):
               return cls
       
    1:     def _default_negotiator(self):
               return api_settings.DEFAULT_CONTENT_NEGOTIATION_CLASS()
       
    2:     @property
    2:     def content_type(self):
   66:         meta = self._request.META
   66:         return meta.get('CONTENT_TYPE', meta.get('HTTP_CONTENT_TYPE', ''))
       
    2:     @property
    2:     def stream(self):
               """
               Returns an object that may be used to stream the request content.
               """
   17:         if not _hasattr(self, '_stream'):
   17:             self._load_stream()
   17:         return self._stream
       
    2:     @property
    2:     def query_params(self):
               """
               More semantically correct name for request.GET.
               """
   23:         return self._request.GET
       
    2:     @property
    2:     def data(self):
   15:         if not _hasattr(self, '_full_data'):
    4:             with wrap_attributeerrors():
    2:                 self._load_data_and_files()
   15:         return self._full_data
       
    2:     @property
    2:     def user(self):
               """
               Returns the user associated with the current request, as authenticated
               by the authentication classes provided to the request.
               """
   51:         if not hasattr(self, '_user'):
   46:             with wrap_attributeerrors():
   23:                 self._authenticate()
   47:         return self._user
       
    2:     @user.setter
    2:     def user(self, value):
               """
               Sets the user on the current request. This is necessary to maintain
               compatibility with django.contrib.auth where the user property is
               set in the login and logout functions.
       
               Note that we also set the user on Django's underlying `HttpRequest`
               instance, ensuring that it is available to any middleware in the stack.
               """
   23:         self._user = value
   23:         self._request.user = value
       
    2:     @property
    2:     def auth(self):
               """
               Returns any non-user authentication information associated with the
               request, such as an authentication token.
               """
               if not hasattr(self, '_auth'):
                   with wrap_attributeerrors():
                       self._authenticate()
               return self._auth
       
    2:     @auth.setter
    2:     def auth(self, value):
               """
               Sets any non-user authentication information associated with the
               request, such as an authentication token.
               """
   23:         self._auth = value
   23:         self._request.auth = value
       
    2:     @property
    2:     def successful_authenticator(self):
               """
               Return the instance of the authentication instance class that was used
               to authenticate the request, or `None`.
               """
               if not hasattr(self, '_authenticator'):
                   with wrap_attributeerrors():
                       self._authenticate()
               return self._authenticator
       
    1:     def _load_data_and_files(self):
               """
               Parses the request content into `self.data`.
               """
   17:         if not _hasattr(self, '_data'):
   17:             self._data, self._files = self._parse()
   17:             if self._files:
                       self._full_data = self._data.copy()
                       self._full_data.update(self._files)
                   else:
   17:                 self._full_data = self._data
       
                   # if a form media type, copy data & files refs to the underlying
                   # http request so that closable objects are handled appropriately.
   17:             if is_form_media_type(self.content_type):
                       self._request._post = self.POST
                       self._request._files = self.FILES
       
    1:     def _load_stream(self):
               """
               Return the content body of the request, as a stream.
               """
   17:         meta = self._request.META
   17:         try:
   34:             content_length = int(
   17:                 meta.get('CONTENT_LENGTH', meta.get('HTTP_CONTENT_LENGTH', 0))
                   )
               except (ValueError, TypeError):
                   content_length = 0
       
   17:         if content_length == 0:
                   self._stream = None
   17:         elif not self._request._read_started:
   17:             self._stream = self._request
               else:
                   self._stream = io.BytesIO(self.body)
       
    1:     def _supports_form_parsing(self):
               """
               Return True if this requests supports parsing form data.
               """
               form_media = (
                   'application/x-www-form-urlencoded',
                   'multipart/form-data'
               )
               return any(parser.media_type in form_media for parser in self.parsers)
       
    1:     def _parse(self):
               """
               Parse the request content, returning a two-tuple of (data, files)
       
               May raise an `UnsupportedMediaType`, or `ParseError` exception.
               """
   17:         media_type = self.content_type
   17:         try:
   17:             stream = self.stream
               except RawPostDataException:
                   if not hasattr(self._request, '_post'):
                       raise
                   # If request.POST has been accessed in middleware, and a method='POST'
                   # request was made with 'multipart/form-data', then the request stream
                   # will already have been exhausted.
                   if self._supports_form_parsing():
                       return (self._request.POST, self._request.FILES)
                   stream = None
       
   17:         if stream is None or media_type is None:
                   if media_type and is_form_media_type(media_type):
                       empty_data = QueryDict('', encoding=self._request._encoding)
                   else:
                       empty_data = {}
                   empty_files = MultiValueDict()
                   return (empty_data, empty_files)
       
   17:         parser = self.negotiator.select_parser(self, self.parsers)
       
   17:         if not parser:
                   raise exceptions.UnsupportedMediaType(media_type)
       
   17:         try:
   17:             parsed = parser.parse(stream, media_type, self.parser_context)
               except Exception:
                   # If we get an exception during parsing, fill in empty data and
                   # re-raise.  Ensures we don't simply repeat the error when
                   # attempting to render the browsable renderer response, or when
                   # logging the request or similar.
                   self._data = QueryDict('', encoding=self._request._encoding)
                   self._files = MultiValueDict()
                   self._full_data = self._data
                   raise
       
               # Parser classes may return the raw data, or a
               # DataAndFiles object.  Unpack the result as required.
   17:         try:
   17:             return (parsed.data, parsed.files)
   17:         except AttributeError:
   17:             empty_files = MultiValueDict()
   17:             return (parsed, empty_files)
       
    1:     def _authenticate(self):
               """
               Attempt to authenticate the request using each authentication instance
               in turn.
               """
   45:         for authenticator in self.authenticators:
   26:             try:
   26:                 user_auth_tuple = authenticator.authenticate(self)
    4:             except exceptions.APIException:
    4:                 self._not_authenticated()
    4:                 raise
       
   22:             if user_auth_tuple is not None:
                       self._authenticator = authenticator
                       self.user, self.auth = user_auth_tuple
                       return
       
   19:         self._not_authenticated()
       
    1:     def _not_authenticated(self):
               """
               Set authenticator, user & authtoken representing an unauthenticated request.
       
               Defaults are None, AnonymousUser & None.
               """
   23:         self._authenticator = None
       
   23:         if api_settings.UNAUTHENTICATED_USER:
   23:             self.user = api_settings.UNAUTHENTICATED_USER()
               else:
                   self.user = None
       
   23:         if api_settings.UNAUTHENTICATED_TOKEN:
                   self.auth = api_settings.UNAUTHENTICATED_TOKEN()
               else:
   23:             self.auth = None
       
    1:     def __getattr__(self, attr):
               """
               If an attribute does not exist on this instance, then we also attempt
               to proxy it to the underlying HttpRequest object.
               """
  341:         try:
  341:             _request = self.__getattribute__("_request")
  341:             return getattr(_request, attr)
   43:         except AttributeError:
   43:             raise AttributeError(f"'{self.__class__.__name__}' object has no attribute '{attr}'")
       
    2:     @property
    2:     def POST(self):
               # Ensure that request.POST uses our request parsing.
   15:         if not _hasattr(self, '_data'):
   30:             with wrap_attributeerrors():
   15:                 self._load_data_and_files()
   15:         if is_form_media_type(self.content_type):
                   return self._data
   15:         return QueryDict('', encoding=self._request._encoding)
       
    2:     @property
    2:     def FILES(self):
               # Leave this one alone for backwards compat with Django's request.FILES
               # Different from the other two cases, which are not valid property
               # names on the WSGIRequest class.
               if not _hasattr(self, '_files'):
                   with wrap_attributeerrors():
                       self._load_data_and_files()
               return self._files
       
    1:     def force_plaintext_errors(self, value):
               # Hack to allow our exception handler to force choice of
               # plaintext or html error responses.
               self._request.is_ajax = lambda: value
