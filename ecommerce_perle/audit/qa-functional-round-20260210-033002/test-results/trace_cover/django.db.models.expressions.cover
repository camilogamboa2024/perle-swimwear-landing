    1: import copy
    1: import datetime
    1: import functools
    1: import inspect
    1: from collections import defaultdict
    1: from decimal import Decimal
    1: from enum import Enum
    1: from itertools import chain
    1: from types import NoneType
    1: from uuid import UUID
       
    1: from django.core.exceptions import EmptyResultSet, FieldError, FullResultSet
    1: from django.db import DatabaseError, NotSupportedError, connection
    1: from django.db.models import fields
    1: from django.db.models.constants import LOOKUP_SEP
    1: from django.db.models.query_utils import Q
    1: from django.utils.deconstruct import deconstructible
    1: from django.utils.functional import cached_property, classproperty
    1: from django.utils.hashable import make_hashable
       
       
    2: class SQLiteNumericMixin:
    1:     """
           Some expressions with output_field=DecimalField() must be cast to
           numeric to be properly filtered.
           """
       
    1:     def as_sqlite(self, compiler, connection, **extra_context):
   32:         sql, params = self.as_sql(compiler, connection, **extra_context)
   32:         try:
   32:             if self.output_field.get_internal_type() == "DecimalField":
                       sql = "(CAST(%s AS NUMERIC))" % sql
               except FieldError:
                   pass
   32:         return sql, params
       
       
    2: class Combinable:
    1:     """
           Provide the ability to combine one or two objects with
           some connector. For example F('foo') + F('bar').
           """
       
           # Arithmetic connectors
    1:     ADD = "+"
    1:     SUB = "-"
    1:     MUL = "*"
    1:     DIV = "/"
    1:     POW = "^"
           # The following is a quoted % operator - it is quoted because it can be
           # used in strings that also have parameter substitution.
    1:     MOD = "%%"
       
           # Bitwise operators - note that these are generated by .bitand()
           # and .bitor(), the '&' and '|' are reserved for boolean operator
           # usage.
    1:     BITAND = "&"
    1:     BITOR = "|"
    1:     BITLEFTSHIFT = "<<"
    1:     BITRIGHTSHIFT = ">>"
    1:     BITXOR = "#"
       
    1:     def _combine(self, other, connector, reversed):
    4:         if not hasattr(other, "resolve_expression"):
                   # everything must be resolvable to an expression
    4:             other = Value(other)
       
    4:         if reversed:
                   return CombinedExpression(other, connector, self)
    4:         return CombinedExpression(self, connector, other)
       
           #############
           # OPERATORS #
           #############
       
    1:     def __neg__(self):
               return self._combine(-1, self.MUL, False)
       
    1:     def __add__(self, other):
               return self._combine(other, self.ADD, False)
       
    1:     def __sub__(self, other):
    4:         return self._combine(other, self.SUB, False)
       
    1:     def __mul__(self, other):
               return self._combine(other, self.MUL, False)
       
    1:     def __truediv__(self, other):
               return self._combine(other, self.DIV, False)
       
    1:     def __mod__(self, other):
               return self._combine(other, self.MOD, False)
       
    1:     def __pow__(self, other):
               return self._combine(other, self.POW, False)
       
    1:     def __and__(self, other):
               if getattr(self, "conditional", False) and getattr(other, "conditional", False):
                   return Q(self) & Q(other)
               raise NotImplementedError(
                   "Use .bitand(), .bitor(), and .bitxor() for bitwise logical operations."
               )
       
    1:     def bitand(self, other):
               return self._combine(other, self.BITAND, False)
       
    1:     def bitleftshift(self, other):
               return self._combine(other, self.BITLEFTSHIFT, False)
       
    1:     def bitrightshift(self, other):
               return self._combine(other, self.BITRIGHTSHIFT, False)
       
    1:     def __xor__(self, other):
               if getattr(self, "conditional", False) and getattr(other, "conditional", False):
                   return Q(self) ^ Q(other)
               raise NotImplementedError(
                   "Use .bitand(), .bitor(), and .bitxor() for bitwise logical operations."
               )
       
    1:     def bitxor(self, other):
               return self._combine(other, self.BITXOR, False)
       
    1:     def __or__(self, other):
               if getattr(self, "conditional", False) and getattr(other, "conditional", False):
                   return Q(self) | Q(other)
               raise NotImplementedError(
                   "Use .bitand(), .bitor(), and .bitxor() for bitwise logical operations."
               )
       
    1:     def bitor(self, other):
               return self._combine(other, self.BITOR, False)
       
    1:     def __radd__(self, other):
               return self._combine(other, self.ADD, True)
       
    1:     def __rsub__(self, other):
               return self._combine(other, self.SUB, True)
       
    1:     def __rmul__(self, other):
               return self._combine(other, self.MUL, True)
       
    1:     def __rtruediv__(self, other):
               return self._combine(other, self.DIV, True)
       
    1:     def __rmod__(self, other):
               return self._combine(other, self.MOD, True)
       
    1:     def __rpow__(self, other):
               return self._combine(other, self.POW, True)
       
    1:     def __rand__(self, other):
               raise NotImplementedError(
                   "Use .bitand(), .bitor(), and .bitxor() for bitwise logical operations."
               )
       
    1:     def __ror__(self, other):
               raise NotImplementedError(
                   "Use .bitand(), .bitor(), and .bitxor() for bitwise logical operations."
               )
       
    1:     def __rxor__(self, other):
               raise NotImplementedError(
                   "Use .bitand(), .bitor(), and .bitxor() for bitwise logical operations."
               )
       
    1:     def __invert__(self):
               return NegatedExpression(self)
       
       
    2: class OutputFieldIsNoneError(FieldError):
    1:     pass
       
       
    2: class BaseExpression:
    1:     """Base class for all query expressions."""
       
    1:     empty_result_set_value = NotImplemented
           # aggregate specific fields
    1:     is_summary = False
           # Can the expression be used in a WHERE clause?
    1:     filterable = True
           # Can the expression be used as a source expression in Window?
    1:     window_compatible = False
           # Can the expression be used as a database default value?
    1:     allowed_default = False
           # Can the expression be used during a constraint validation?
    1:     constraint_validation_compatible = True
           # Does the expression possibly return more than one row?
    1:     set_returning = False
           # Does the expression allow composite expressions?
    1:     allows_composite_expressions = False
       
    1:     def __init__(self, output_field=None):
  732:         if output_field is not None:
  594:             self.output_field = output_field
       
    1:     def __getstate__(self):
  524:         state = self.__dict__.copy()
  524:         state.pop("convert_value", None)
  524:         return state
       
    1:     def get_db_converters(self, connection):
    2:         return (
                   []
    2:             if self.convert_value is self._convert_value_noop
    2:             else [self.convert_value]
    2:         ) + self.output_field.get_db_converters(connection)
       
    1:     def get_source_expressions(self):
  566:         return []
       
    1:     def set_source_expressions(self, exprs):
  474:         assert not exprs
       
    1:     def _parse_expressions(self, *expressions):
   10:         return [
                   (
    2:                 arg
    2:                 if hasattr(arg, "resolve_expression")
                       else (F(arg) if isinstance(arg, str) else Value(arg))
                   )
    4:             for arg in expressions
               ]
       
    1:     def as_sql(self, compiler, connection):
               """
               Responsible for returning a (sql, [params]) tuple to be included
               in the current query.
       
               Different backends can provide their own implementation, by
               providing an `as_{vendor}` method and patching the Expression:
       
               ```
               def override_as_sql(self, compiler, connection):
                   # custom logic
                   return super().as_sql(compiler, connection)
               setattr(Expression, 'as_' + connection.vendor, override_as_sql)
               ```
       
               Arguments:
                * compiler: the query compiler responsible for generating the query.
                  Must have a compile method, returning a (sql, [params]) tuple.
                  Calling compiler(value) will return a quoted `value`.
       
                * connection: the database connection used for the current query.
       
               Return: (sql, params)
                 Where `sql` is a string containing ordered sql parameters to be
                 replaced with the elements of the list `params`.
               """
               raise NotImplementedError("Subclasses must implement as_sql()")
       
    2:     @cached_property
    2:     def contains_aggregate(self):
 1845:         return any(
  730:             expr and expr.contains_aggregate for expr in self.get_source_expressions()
               )
       
    2:     @cached_property
    2:     def contains_over_clause(self):
 1845:         return any(
  730:             expr and expr.contains_over_clause for expr in self.get_source_expressions()
               )
       
    2:     @cached_property
    2:     def contains_column_references(self):
               return any(
                   expr and expr.contains_column_references
                   for expr in self.get_source_expressions()
               )
       
    2:     @cached_property
    2:     def contains_subquery(self):
               return any(
                   expr and (getattr(expr, "subquery", False) or expr.contains_subquery)
                   for expr in self.get_source_expressions()
               )
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               """
               Provide the chance to do any preprocessing or validation before being
               added to the query.
       
               Arguments:
                * query: the backend query implementation
                * allow_joins: boolean allowing or denying use of joins
                  in this query
                * reuse: a set of reusable joins for multijoins
                * summarize: a terminal aggregate clause
                * for_save: whether this expression about to be used in a save or update
       
               Return: an Expression to be added to the query.
               """
  524:         c = self.copy()
  524:         c.is_summary = summarize
 1692:         source_expressions = [
                   (
   58:                 expr.resolve_expression(query, allow_joins, reuse, summarize, for_save)
   60:                 if expr is not None
    2:                 else None
                   )
  584:             for expr in c.get_source_expressions()
               ]
 1520:         if not self.allows_composite_expressions and any(
  498:             isinstance(expr, ColPairs) for expr in source_expressions
               ):
                   raise ValueError(
                       f"{self.__class__.__name__} expression does not support "
                       "composite primary keys."
                   )
  524:         c.set_source_expressions(source_expressions)
  524:         return c
       
    2:     @property
    2:     def conditional(self):
   34:         return isinstance(self.output_field, fields.BooleanField)
       
    2:     @property
    2:     def field(self):
               return self.output_field
       
    2:     @cached_property
    2:     def output_field(self):
               """Return the output type of this expressions."""
   30:         output_field = self._resolve_output_field()
   30:         if output_field is None:
                   raise OutputFieldIsNoneError(
                       "Cannot resolve expression type, unknown output_field"
                   )
   30:         return output_field
       
    2:     @property
    2:     def _output_field_or_none(self):
               """
               Return the output field of this expression, or None if
               _resolve_output_field() didn't return an output type.
               """
   34:         try:
   34:             return self.output_field
               except OutputFieldIsNoneError:
                   return
       
    1:     def _resolve_output_field(self):
               """
               Attempt to infer the output type of the expression.
       
               As a guess, if the output fields of all source fields match then simply
               infer the same type here.
       
               If a source's output field resolves to None, exclude it from this check.
               If all sources are None, then an error is raised higher up the stack in
               the output_field property.
               """
               # This guess is mostly a bad idea, but there is quite a lot of code
               # (especially 3rd party Func subclasses) that depend on it, we'd need a
               # deprecation path to fix it.
               sources_iter = (
                   source for source in self.get_source_fields() if source is not None
               )
               for output_field in sources_iter:
                   for source in sources_iter:
                       if not isinstance(output_field, source.__class__):
                           raise FieldError(
                               "Expression contains mixed types: %s, %s. You must "
                               "set output_field."
                               % (
                                   output_field.__class__.__name__,
                                   source.__class__.__name__,
                               )
                           )
                   return output_field
       
    2:     @staticmethod
    2:     def _convert_value_noop(value, expression, connection):
               return value
       
    2:     @cached_property
    2:     def convert_value(self):
               """
               Expressions provide their own converters because users have the option
               of manually specifying the output_field which may be a different type
               from the one the database returns.
               """
    2:         field = self.output_field
    2:         internal_type = field.get_internal_type()
    2:         if internal_type == "FloatField":
                   return lambda value, expression, connection: (
                       None if value is None else float(value)
                   )
    2:         elif internal_type.endswith("IntegerField"):
    4:             return lambda value, expression, connection: (
    2:                 None if value is None else int(value)
                   )
               elif internal_type == "DecimalField":
                   return lambda value, expression, connection: (
                       None if value is None else Decimal(value)
                   )
               return self._convert_value_noop
       
    1:     def get_lookup(self, lookup):
  412:         return self.output_field.get_lookup(lookup)
       
    1:     def get_transform(self, name):
               return self.output_field.get_transform(name)
       
    1:     def relabeled_clone(self, change_map):
               clone = self.copy()
               clone.set_source_expressions(
                   [
                       e.relabeled_clone(change_map) if e is not None else None
                       for e in self.get_source_expressions()
                   ]
               )
               return clone
       
    1:     def replace_expressions(self, replacements):
    2:         if not replacements:
    2:             return self
               if replacement := replacements.get(self):
                   return replacement
               if not (source_expressions := self.get_source_expressions()):
                   return self
               clone = self.copy()
               clone.set_source_expressions(
                   [
                       None if expr is None else expr.replace_expressions(replacements)
                       for expr in source_expressions
                   ]
               )
               return clone
       
    1:     def get_refs(self):
    8:         refs = set()
   16:         for expr in self.get_source_expressions():
    8:             if expr is None:
    4:                 continue
    4:             refs |= expr.get_refs()
    8:         return refs
       
    1:     def copy(self):
  524:         return copy.copy(self)
       
    1:     def prefix_references(self, prefix):
               clone = self.copy()
               clone.set_source_expressions(
                   [
                       (
                           F(f"{prefix}{expr.name}")
                           if isinstance(expr, F)
                           else expr.prefix_references(prefix)
                       )
                       for expr in self.get_source_expressions()
                   ]
               )
               return clone
       
    1:     def get_group_by_cols(self):
               if not self.contains_aggregate:
                   return [self]
               cols = []
               for source in self.get_source_expressions():
                   cols.extend(source.get_group_by_cols())
               return cols
       
    1:     def get_source_fields(self):
               """Return the underlying field types used by this aggregate."""
               return [e._output_field_or_none for e in self.get_source_expressions()]
       
    1:     def asc(self, **kwargs):
               return OrderBy(self, **kwargs)
       
    1:     def desc(self, **kwargs):
               return OrderBy(self, descending=True, **kwargs)
       
    1:     def reverse_ordering(self):
               return self
       
    1:     def flatten(self):
               """
               Recursively yield this expression and all subexpressions, in
               depth-first order.
               """
               yield self
               for expr in self.get_source_expressions():
                   if expr:
                       if hasattr(expr, "flatten"):
                           yield from expr.flatten()
                       else:
                           yield expr
       
    1:     def select_format(self, compiler, sql, params):
               """
               Custom format for select clauses. For example, EXISTS expressions need
               to be wrapped in CASE WHEN on Oracle.
               """
 1850:         if hasattr(self.output_field, "select_format"):
 1850:             return self.output_field.select_format(compiler, sql, params)
               return sql, params
       
    1:     def get_expression_for_validation(self):
               # Ignore expressions that cannot be used during a constraint validation.
               if not getattr(self, "constraint_validation_compatible", True):
                   try:
                       (expression,) = self.get_source_expressions()
                   except ValueError as e:
                       raise ValueError(
                           "Expressions with constraint_validation_compatible set to False "
                           "must have only one source expression."
                       ) from e
                   else:
                       return expression
               return self
       
       
    3: @deconstructible
    2: class Expression(BaseExpression, Combinable):
    1:     """An expression that can be combined with other expressions."""
       
    2:     @classproperty
    2:     @functools.lru_cache(maxsize=128)
    2:     def _constructor_signature(cls):
    3:         return inspect.signature(cls.__init__)
       
    2:     @classmethod
    2:     def _identity(cls, value):
  112:         if isinstance(value, tuple):
                   return tuple(map(cls._identity, value))
  112:         if isinstance(value, dict):
    4:             return tuple((key, cls._identity(val)) for key, val in value.items())
  110:         if isinstance(value, fields.Field):
   58:             if value.name and value.model:
   58:                 return value.model._meta.label, value.name
                   return type(value)
   52:         return make_hashable(value)
       
    2:     @cached_property
    2:     def identity(self):
   38:         args, kwargs = self._constructor_args
   38:         signature = self._constructor_signature.bind_partial(self, *args, **kwargs)
   38:         signature.apply_defaults()
   38:         arguments = iter(signature.arguments.items())
   38:         next(arguments)
   38:         identity = [self.__class__]
  150:         for arg, value in arguments:
                   # If __init__() makes use of *args or **kwargs captures `value`
                   # will respectively be a tuple or a dict that must have its
                   # constituents unpacked (mainly if contain Field instances).
  112:             value = self._identity(value)
  112:             identity.append((arg, value))
   38:         return tuple(identity)
       
    1:     def __eq__(self, other):
               if not isinstance(other, Expression):
                   return NotImplemented
               return other.identity == self.identity
       
    1:     def __hash__(self):
  106:         return hash(self.identity)
       
       
       # Type inference for CombinedExpression.output_field.
       # Missing items will result in FieldError, by design.
       #
       # The current approach for NULL is based on lowest common denominator behavior
       # i.e. if one of the supported databases is raising an error (rather than
       # return NULL) for `val <op> NULL`, then Django raises FieldError.
       
    1: _connector_combinations = [
           # Numeric operations - operands of same type.
           # PositiveIntegerField should take precedence over IntegerField (except
           # subtraction).
   13:     {
   10:         connector: [
    5:             (
    5:                 fields.PositiveIntegerField,
    5:                 fields.PositiveIntegerField,
    5:                 fields.PositiveIntegerField,
                   ),
               ]
    6:         for connector in (
    1:             Combinable.ADD,
    1:             Combinable.MUL,
    1:             Combinable.DIV,
    1:             Combinable.MOD,
    1:             Combinable.POW,
               )
           },
           # Other numeric operands.
   15:     {
   12:         connector: [
    6:             (fields.IntegerField, fields.IntegerField, fields.IntegerField),
    6:             (fields.FloatField, fields.FloatField, fields.FloatField),
    6:             (fields.DecimalField, fields.DecimalField, fields.DecimalField),
               ]
    7:         for connector in (
    1:             Combinable.ADD,
    1:             Combinable.SUB,
    1:             Combinable.MUL,
                   # Behavior for DIV with integer arguments follows Postgres/SQLite,
                   # not MySQL/Oracle.
    1:             Combinable.DIV,
    1:             Combinable.MOD,
    1:             Combinable.POW,
               )
           },
           # Numeric operations - operands of different type.
   13:     {
   10:         connector: [
    5:             (fields.IntegerField, fields.DecimalField, fields.DecimalField),
    5:             (fields.DecimalField, fields.IntegerField, fields.DecimalField),
    5:             (fields.IntegerField, fields.FloatField, fields.FloatField),
    5:             (fields.FloatField, fields.IntegerField, fields.FloatField),
               ]
    6:         for connector in (
    1:             Combinable.ADD,
    1:             Combinable.SUB,
    1:             Combinable.MUL,
    1:             Combinable.DIV,
    1:             Combinable.MOD,
               )
           },
           # Bitwise operators.
   13:     {
   10:         connector: [
    5:             (fields.IntegerField, fields.IntegerField, fields.IntegerField),
               ]
    6:         for connector in (
    1:             Combinable.BITAND,
    1:             Combinable.BITOR,
    1:             Combinable.BITLEFTSHIFT,
    1:             Combinable.BITRIGHTSHIFT,
    1:             Combinable.BITXOR,
               )
           },
           # Numeric with NULL.
   15:     {
   12:         connector: list(
   54:             chain.from_iterable(
   18:                 [(field_type, NoneType, field_type), (NoneType, field_type, field_type)]
   24:                 for field_type in (
    6:                     fields.IntegerField,
    6:                     fields.DecimalField,
    6:                     fields.FloatField,
                       )
                   )
               )
    7:         for connector in (
    1:             Combinable.ADD,
    1:             Combinable.SUB,
    1:             Combinable.MUL,
    1:             Combinable.DIV,
    1:             Combinable.MOD,
    1:             Combinable.POW,
               )
           },
           # Date/DateTimeField/DurationField/TimeField.
    1:     {
    2:         Combinable.ADD: [
                   # Date/DateTimeField.
    1:             (fields.DateField, fields.DurationField, fields.DateTimeField),
    1:             (fields.DateTimeField, fields.DurationField, fields.DateTimeField),
    1:             (fields.DurationField, fields.DateField, fields.DateTimeField),
    1:             (fields.DurationField, fields.DateTimeField, fields.DateTimeField),
                   # DurationField.
    1:             (fields.DurationField, fields.DurationField, fields.DurationField),
                   # TimeField.
    1:             (fields.TimeField, fields.DurationField, fields.TimeField),
    1:             (fields.DurationField, fields.TimeField, fields.TimeField),
               ],
           },
    1:     {
    2:         Combinable.SUB: [
                   # Date/DateTimeField.
    1:             (fields.DateField, fields.DurationField, fields.DateTimeField),
    1:             (fields.DateTimeField, fields.DurationField, fields.DateTimeField),
    1:             (fields.DateField, fields.DateField, fields.DurationField),
    1:             (fields.DateField, fields.DateTimeField, fields.DurationField),
    1:             (fields.DateTimeField, fields.DateField, fields.DurationField),
    1:             (fields.DateTimeField, fields.DateTimeField, fields.DurationField),
                   # DurationField.
    1:             (fields.DurationField, fields.DurationField, fields.DurationField),
                   # TimeField.
    1:             (fields.TimeField, fields.DurationField, fields.TimeField),
    1:             (fields.TimeField, fields.TimeField, fields.DurationField),
               ],
           },
       ]
       
    1: _connector_combinators = defaultdict(list)
       
       
    1: def register_combinable_fields(lhs, connector, rhs, result):
           """
           Register combinable types:
               lhs <connector> rhs -> result
           e.g.
               register_combinable_fields(
                   IntegerField, Combinable.ADD, FloatField, FloatField
               )
           """
  101:     _connector_combinators[connector].append((lhs, rhs, result))
       
       
    8: for d in _connector_combinations:
   36:     for connector, field_types in d.items():
  129:         for lhs, rhs, result in field_types:
  100:             register_combinable_fields(lhs, connector, rhs, result)
       
       
    2: @functools.lru_cache(maxsize=128)
    2: def _resolve_combined_type(connector, lhs_type, rhs_type):
    1:     combinators = _connector_combinators.get(connector, ())
    1:     for combinator_lhs_type, combinator_rhs_type, combined_type in combinators:
    2:         if issubclass(lhs_type, combinator_lhs_type) and issubclass(
    1:             rhs_type, combinator_rhs_type
               ):
    1:             return combined_type
       
       
    2: class CombinedExpression(SQLiteNumericMixin, Expression):
    1:     def __init__(self, lhs, connector, rhs, output_field=None):
    4:         super().__init__(output_field=output_field)
    4:         self.connector = connector
    4:         self.lhs = lhs
    4:         self.rhs = rhs
       
    1:     def __repr__(self):
               return "<{}: {}>".format(self.__class__.__name__, self)
       
    1:     def __str__(self):
               return "{} {} {}".format(self.lhs, self.connector, self.rhs)
       
    1:     def get_source_expressions(self):
   16:         return [self.lhs, self.rhs]
       
    1:     def set_source_expressions(self, exprs):
    8:         self.lhs, self.rhs = exprs
       
    1:     def _resolve_output_field(self):
               # We avoid using super() here for reasons given in
               # Expression._resolve_output_field()
    8:         combined_type = _resolve_combined_type(
    4:             self.connector,
    4:             type(self.lhs._output_field_or_none),
    4:             type(self.rhs._output_field_or_none),
               )
    4:         if combined_type is None:
                   raise FieldError(
                       f"Cannot infer type of {self.connector!r} expression involving these "
                       f"types: {self.lhs.output_field.__class__.__name__}, "
                       f"{self.rhs.output_field.__class__.__name__}. You must set "
                       f"output_field."
                   )
    4:         return combined_type()
       
    1:     def as_sql(self, compiler, connection):
    4:         expressions = []
    4:         expression_params = []
    4:         sql, params = compiler.compile(self.lhs)
    4:         expressions.append(sql)
    4:         expression_params.extend(params)
    4:         sql, params = compiler.compile(self.rhs)
    4:         expressions.append(sql)
    4:         expression_params.extend(params)
               # order of precedence
    4:         expression_wrapper = "(%s)"
    4:         sql = connection.ops.combine_expression(self.connector, expressions)
    4:         return expression_wrapper % sql, expression_params
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
   16:         resolved = super().resolve_expression(
    8:             query,
    8:             allow_joins,
    8:             reuse,
    8:             summarize,
    8:             for_save,
               )
    8:         if not isinstance(self, (DurationExpression, TemporalSubtraction)):
    8:             try:
    8:                 lhs_type = resolved.lhs.output_field.get_internal_type()
                   except (AttributeError, FieldError):
                       lhs_type = None
    8:             try:
    8:                 rhs_type = resolved.rhs.output_field.get_internal_type()
                   except (AttributeError, FieldError):
                       rhs_type = None
    8:             if "DurationField" in {lhs_type, rhs_type} and lhs_type != rhs_type:
                       return DurationExpression(
                           resolved.lhs, resolved.connector, resolved.rhs
                       )
    8:             datetime_fields = {"DateField", "DateTimeField", "TimeField"}
                   if (
    8:                 self.connector == self.SUB
    8:                 and lhs_type in datetime_fields
                       and lhs_type == rhs_type
                   ):
                       return TemporalSubtraction(resolved.lhs, resolved.rhs)
    8:         return resolved
       
    2:     @cached_property
    2:     def allowed_default(self):
               return self.lhs.allowed_default and self.rhs.allowed_default
       
       
    2: class DurationExpression(CombinedExpression):
    1:     def compile(self, side, compiler, connection):
               try:
                   output = side.output_field
               except FieldError:
                   pass
               else:
                   if output.get_internal_type() == "DurationField":
                       sql, params = compiler.compile(side)
                       return connection.ops.format_for_duration_arithmetic(sql), params
               return compiler.compile(side)
       
    1:     def as_sql(self, compiler, connection):
               if connection.features.has_native_duration_field:
                   return super().as_sql(compiler, connection)
               connection.ops.check_expression_support(self)
               expressions = []
               expression_params = []
               sql, params = self.compile(self.lhs, compiler, connection)
               expressions.append(sql)
               expression_params.extend(params)
               sql, params = self.compile(self.rhs, compiler, connection)
               expressions.append(sql)
               expression_params.extend(params)
               # order of precedence
               expression_wrapper = "(%s)"
               sql = connection.ops.combine_duration_expression(self.connector, expressions)
               return expression_wrapper % sql, expression_params
       
    1:     def as_sqlite(self, compiler, connection, **extra_context):
               sql, params = self.as_sql(compiler, connection, **extra_context)
               if self.connector in {Combinable.MUL, Combinable.DIV}:
                   try:
                       lhs_type = self.lhs.output_field.get_internal_type()
                       rhs_type = self.rhs.output_field.get_internal_type()
                   except (AttributeError, FieldError):
                       pass
                   else:
                       allowed_fields = {
                           "DecimalField",
                           "DurationField",
                           "FloatField",
                           "IntegerField",
                       }
                       if lhs_type not in allowed_fields or rhs_type not in allowed_fields:
                           raise DatabaseError(
                               f"Invalid arguments for operator {self.connector}."
                           )
               return sql, params
       
       
    2: class TemporalSubtraction(CombinedExpression):
    1:     output_field = fields.DurationField()
       
    1:     def __init__(self, lhs, rhs):
               super().__init__(lhs, self.SUB, rhs)
       
    1:     def as_sql(self, compiler, connection):
               connection.ops.check_expression_support(self)
               lhs = compiler.compile(self.lhs)
               rhs = compiler.compile(self.rhs)
               return connection.ops.subtract_temporals(
                   self.lhs.output_field.get_internal_type(), lhs, rhs
               )
       
       
    3: @deconstructible(path="django.db.models.F")
    2: class F(Combinable):
    1:     """An object capable of resolving references to existing query objects."""
       
    1:     allowed_default = False
       
    1:     def __init__(self, name):
               """
               Arguments:
                * name: the name of the field this expression references
               """
    4:         self.name = name
       
    1:     def __repr__(self):
               return "{}({})".format(self.__class__.__name__, self.name)
       
    1:     def __getitem__(self, subscript):
               return Sliced(self, subscript)
       
    1:     def __contains__(self, other):
               # Disable old-style iteration protocol inherited from implementing
               # __getitem__() to prevent this method from hanging.
               raise TypeError(f"argument of type '{self.__class__.__name__}' is not iterable")
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
    4:         return query.resolve_ref(self.name, allow_joins, reuse, summarize)
       
    1:     def replace_expressions(self, replacements):
               if (replacement := replacements.get(self)) is not None:
                   return replacement
               field_name, *transforms = self.name.split(LOOKUP_SEP)
               # Avoid unnecessarily looking up replacements with field_name again as
               # in the vast majority of cases F instances won't be composed of any
               # lookups.
               if not transforms:
                   return self
               if (
                   replacement := replacements.get(F(field_name))
               ) is None or replacement._output_field_or_none is None:
                   return self
               for transform in transforms:
                   transform_class = replacement.get_transform(transform)
                   if transform_class is None:
                       return self
                   replacement = transform_class(replacement)
               return replacement
       
    1:     def asc(self, **kwargs):
               return OrderBy(self, **kwargs)
       
    1:     def desc(self, **kwargs):
               return OrderBy(self, descending=True, **kwargs)
       
    1:     def __eq__(self, other):
               return self.__class__ == other.__class__ and self.name == other.name
       
    1:     def __hash__(self):
               return hash(self.name)
       
    1:     def copy(self):
               return copy.copy(self)
       
       
    2: class ResolvedOuterRef(F):
    1:     """
           An object that contains a reference to an outer query.
       
           In this case, the reference to the outer query has been resolved because
           the inner query has been used as a subquery.
           """
       
    1:     contains_aggregate = False
    1:     contains_over_clause = False
       
    1:     def as_sql(self, *args, **kwargs):
               raise ValueError(
                   "This queryset contains a reference to an outer query and may "
                   "only be used in a subquery."
               )
       
    1:     def resolve_expression(self, *args, **kwargs):
               col = super().resolve_expression(*args, **kwargs)
               if col.contains_over_clause:
                   raise NotSupportedError(
                       f"Referencing outer query window expression is not supported: "
                       f"{self.name}."
                   )
               # FIXME: Rename possibly_multivalued to multivalued and fix detection
               # for non-multivalued JOINs (e.g. foreign key fields). This should take
               # into account only many-to-many and one-to-many relationships.
               col.possibly_multivalued = LOOKUP_SEP in self.name
               return col
       
    1:     def relabeled_clone(self, relabels):
               return self
       
    1:     def get_group_by_cols(self):
               return []
       
       
    2: class OuterRef(F):
    1:     contains_aggregate = False
    1:     contains_over_clause = False
       
    1:     def resolve_expression(self, *args, **kwargs):
               if isinstance(self.name, self.__class__):
                   return self.name
               return ResolvedOuterRef(self.name)
       
    1:     def relabeled_clone(self, relabels):
               return self
       
       
    2: class Sliced(F):
    1:     """
           An object that contains a slice of an F expression.
       
           Object resolves the column on which the slicing is applied, and then
           applies the slicing if possible.
           """
       
    1:     def __init__(self, obj, subscript):
               super().__init__(obj.name)
               self.obj = obj
               if isinstance(subscript, int):
                   if subscript < 0:
                       raise ValueError("Negative indexing is not supported.")
                   self.start = subscript + 1
                   self.length = 1
               elif isinstance(subscript, slice):
                   if (subscript.start is not None and subscript.start < 0) or (
                       subscript.stop is not None and subscript.stop < 0
                   ):
                       raise ValueError("Negative indexing is not supported.")
                   if subscript.step is not None:
                       raise ValueError("Step argument is not supported.")
                   if subscript.stop and subscript.start and subscript.stop < subscript.start:
                       raise ValueError("Slice stop must be greater than slice start.")
                   self.start = 1 if subscript.start is None else subscript.start + 1
                   if subscript.stop is None:
                       self.length = None
                   else:
                       self.length = subscript.stop - (subscript.start or 0)
               else:
                   raise TypeError("Argument to slice must be either int or slice instance.")
       
    1:     def __repr__(self):
               start = self.start - 1
               stop = None if self.length is None else start + self.length
               subscript = slice(start, stop)
               return f"{self.__class__.__qualname__}({self.obj!r}, {subscript!r})"
       
    1:     def resolve_expression(
               self,
    1:         query=None,
    1:         allow_joins=True,
    1:         reuse=None,
    1:         summarize=False,
    1:         for_save=False,
           ):
               resolved = query.resolve_ref(self.name, allow_joins, reuse, summarize)
               if isinstance(self.obj, (OuterRef, self.__class__)):
                   expr = self.obj.resolve_expression(
                       query, allow_joins, reuse, summarize, for_save
                   )
               else:
                   expr = resolved
               return resolved.output_field.slice_expression(expr, self.start, self.length)
       
       
    3: @deconstructible(path="django.db.models.Func")
    2: class Func(SQLiteNumericMixin, Expression):
    1:     """An SQL function call."""
       
    1:     function = None
    1:     template = "%(function)s(%(expressions)s)"
    1:     arg_joiner = ", "
    1:     arity = None  # The number of arguments the function accepts.
       
    1:     def __init__(self, *expressions, output_field=None, **extra):
    2:         if self.arity is not None and len(expressions) != self.arity:
                   raise TypeError(
                       "'%s' takes exactly %s %s (%s given)"
                       % (
                           self.__class__.__name__,
                           self.arity,
                           "argument" if self.arity == 1 else "arguments",
                           len(expressions),
                       )
                   )
    2:         super().__init__(output_field=output_field)
    2:         self.source_expressions = self._parse_expressions(*expressions)
    2:         self.extra = extra
       
    1:     def __repr__(self):
               args = self.arg_joiner.join(str(arg) for arg in self.source_expressions)
               extra = {**self.extra, **self._get_repr_options()}
               if extra:
                   extra = ", ".join(
                       str(key) + "=" + str(val) for key, val in sorted(extra.items())
                   )
                   return "{}({}, {})".format(self.__class__.__name__, args, extra)
               return "{}({})".format(self.__class__.__name__, args)
       
    1:     def _get_repr_options(self):
               """Return a dict of extra __init__() options to include in the repr."""
               return {}
       
    1:     def get_source_expressions(self):
    6:         return self.source_expressions
       
    1:     def set_source_expressions(self, exprs):
    2:         self.source_expressions = exprs
       
    1:     def as_sql(
               self,
               compiler,
               connection,
    1:         function=None,
    1:         template=None,
    1:         arg_joiner=None,
               **extra_context,
           ):
    2:         connection.ops.check_expression_support(self)
    2:         sql_parts = []
    2:         params = []
    4:         for arg in self.source_expressions:
    2:             try:
    2:                 arg_sql, arg_params = compiler.compile(arg)
                   except EmptyResultSet:
                       empty_result_set_value = getattr(
                           arg, "empty_result_set_value", NotImplemented
                       )
                       if empty_result_set_value is NotImplemented:
                           raise
                       arg_sql, arg_params = compiler.compile(Value(empty_result_set_value))
                   except FullResultSet:
                       arg_sql, arg_params = compiler.compile(Value(True))
    2:             sql_parts.append(arg_sql)
    2:             params.extend(arg_params)
    2:         data = {**self.extra, **extra_context}
               # Use the first supplied value in this order: the parameter to this
               # method, a value supplied in __init__()'s **extra (the value in
               # `data`), or the value defined on the class.
    2:         if function is not None:
                   data["function"] = function
               else:
    2:             data.setdefault("function", self.function)
    2:         template = template or data.get("template", self.template)
    2:         arg_joiner = arg_joiner or data.get("arg_joiner", self.arg_joiner)
    2:         data["expressions"] = data["field"] = arg_joiner.join(sql_parts)
    2:         return template % data, params
       
    1:     def copy(self):
    2:         copy = super().copy()
    2:         copy.source_expressions = self.source_expressions[:]
    2:         copy.extra = self.extra.copy()
    2:         return copy
       
    2:     @cached_property
    2:     def allowed_default(self):
               return all(expression.allowed_default for expression in self.source_expressions)
       
       
    3: @deconstructible(path="django.db.models.Value")
    2: class Value(SQLiteNumericMixin, Expression):
    1:     """Represent a wrapped value as a node within an expression."""
       
           # Provide a default value for `for_save` in order to allow unresolved
           # instances to be compiled until a decision is taken in #25425.
    1:     for_save = False
    1:     allowed_default = True
       
    1:     def __init__(self, value, output_field=None):
               """
               Arguments:
                * value: the value this expression represents. The value will be
                  added into the sql parameter list and properly quoted.
       
                * output_field: an instance of the model field type that this
                  expression will return, such as IntegerField() or CharField().
               """
   28:         super().__init__(output_field=output_field)
   28:         self.value = value
       
    1:     def __repr__(self):
               return f"{self.__class__.__name__}({self.value!r})"
       
    1:     def as_sql(self, compiler, connection):
   26:         connection.ops.check_expression_support(self)
   26:         val = self.value
   26:         output_field = self._output_field_or_none
   26:         if output_field is not None:
   26:             if self.for_save:
    4:                 val = output_field.get_db_prep_save(val, connection=connection)
                   else:
   22:                 val = output_field.get_db_prep_value(val, connection=connection)
   26:             if hasattr(output_field, "get_placeholder"):
                       return output_field.get_placeholder(val, compiler, connection), [val]
   26:         if val is None:
                   # oracledb does not always convert None to the appropriate
                   # NULL type (like in case expressions using numbers), so we
                   # use a literal SQL NULL
                   return "NULL", []
   26:         return "%s", [val]
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
   30:         c = super().resolve_expression(query, allow_joins, reuse, summarize, for_save)
   30:         c.for_save = for_save
   30:         return c
       
    1:     def get_group_by_cols(self):
               return []
       
    1:     def _resolve_output_field(self):
   26:         if isinstance(self.value, str):
                   return fields.CharField()
   26:         if isinstance(self.value, bool):
                   return fields.BooleanField()
   26:         if isinstance(self.value, int):
   26:             return fields.IntegerField()
               if isinstance(self.value, float):
                   return fields.FloatField()
               if isinstance(self.value, datetime.datetime):
                   return fields.DateTimeField()
               if isinstance(self.value, datetime.date):
                   return fields.DateField()
               if isinstance(self.value, datetime.time):
                   return fields.TimeField()
               if isinstance(self.value, datetime.timedelta):
                   return fields.DurationField()
               if isinstance(self.value, Decimal):
                   return fields.DecimalField()
               if isinstance(self.value, bytes):
                   return fields.BinaryField()
               if isinstance(self.value, UUID):
                   return fields.UUIDField()
       
    2:     @property
    2:     def empty_result_set_value(self):
               return self.value
       
       
    2: class RawSQL(Expression):
    1:     allowed_default = True
       
    1:     def __init__(self, sql, params, output_field=None):
               if output_field is None:
                   output_field = fields.Field()
               self.sql, self.params = sql, params
               super().__init__(output_field=output_field)
       
    1:     def __repr__(self):
               return "{}({}, {})".format(self.__class__.__name__, self.sql, self.params)
       
    1:     def as_sql(self, compiler, connection):
               return "(%s)" % self.sql, self.params
       
    1:     def get_group_by_cols(self):
               return [self]
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               # Resolve parents fields used in raw SQL.
               if query.model:
                   for parent in query.model._meta.all_parents:
                       for parent_field in parent._meta.local_fields:
                           if parent_field.column.lower() in self.sql.lower():
                               query.resolve_ref(
                                   parent_field.name, allow_joins, reuse, summarize
                               )
                               break
               return super().resolve_expression(
                   query, allow_joins, reuse, summarize, for_save
               )
       
       
    2: class Star(Expression):
    1:     def __repr__(self):
               return "'*'"
       
    1:     def as_sql(self, compiler, connection):
    2:         return "*", []
       
       
    2: class DatabaseDefault(Expression):
    1:     """
           Expression to use DEFAULT keyword during insert otherwise the underlying expression.
           """
       
    1:     def __init__(self, expression, output_field=None):
               super().__init__(output_field)
               self.expression = expression
       
    1:     def get_source_expressions(self):
               return [self.expression]
       
    1:     def set_source_expressions(self, exprs):
               (self.expression,) = exprs
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               resolved_expression = self.expression.resolve_expression(
                   query=query,
                   allow_joins=allow_joins,
                   reuse=reuse,
                   summarize=summarize,
                   for_save=for_save,
               )
               # Defaults used outside an INSERT context should resolve to their
               # underlying expression.
               if not for_save:
                   return resolved_expression
               return DatabaseDefault(
                   resolved_expression, output_field=self._output_field_or_none
               )
       
    1:     def as_sql(self, compiler, connection):
               if not connection.features.supports_default_keyword_in_insert:
                   return compiler.compile(self.expression)
               return "DEFAULT", []
       
       
    2: class Col(Expression):
    1:     contains_column_references = True
    1:     possibly_multivalued = False
       
    1:     def __init__(self, alias, target, output_field=None):
  594:         if output_field is None:
  241:             output_field = target
  594:         super().__init__(output_field=output_field)
  594:         self.alias, self.target = alias, target
       
    1:     def __repr__(self):
               alias, target = self.alias, self.target
               identifiers = (alias, str(target)) if alias else (str(target),)
               return "{}({})".format(self.__class__.__name__, ", ".join(identifiers))
       
    1:     def as_sql(self, compiler, connection):
 2432:         alias, column = self.alias, self.target.column
 2432:         identifiers = (alias, column) if alias else (column,)
 2432:         sql = ".".join(map(compiler.quote_name_unless_alias, identifiers))
 2432:         return sql, []
       
    1:     def relabeled_clone(self, relabels):
               if self.alias is None:
                   return self
               return self.__class__(
                   relabels.get(self.alias, self.alias), self.target, self.output_field
               )
       
    1:     def get_group_by_cols(self):
               return [self]
       
    1:     def get_db_converters(self, connection):
 2031:         if self.target == self.output_field:
 1688:             return self.output_field.get_db_converters(connection)
 1029:         return self.output_field.get_db_converters(
  343:             connection
  343:         ) + self.target.get_db_converters(connection)
       
       
    2: class ColPairs(Expression):
    1:     def __init__(self, alias, targets, sources, output_field):
               super().__init__(output_field=output_field)
               self.alias = alias
               self.targets = targets
               self.sources = sources
       
    1:     def __len__(self):
               return len(self.targets)
       
    1:     def __iter__(self):
               return iter(self.get_cols())
       
    1:     def __repr__(self):
               return (
                   f"{self.__class__.__name__}({self.alias!r}, {self.targets!r}, "
                   f"{self.sources!r}, {self.output_field!r})"
               )
       
    1:     def get_cols(self):
               return [
                   Col(self.alias, target, source)
                   for target, source in zip(self.targets, self.sources)
               ]
       
    1:     def get_source_expressions(self):
               return self.get_cols()
       
    1:     def set_source_expressions(self, exprs):
               assert all(isinstance(expr, Col) and expr.alias == self.alias for expr in exprs)
               self.targets = [col.target for col in exprs]
               self.sources = [col.field for col in exprs]
       
    1:     def as_sql(self, compiler, connection):
               cols_sql = []
               cols_params = []
               cols = self.get_cols()
       
               for col in cols:
                   sql, params = col.as_sql(compiler, connection)
                   cols_sql.append(sql)
                   cols_params.extend(params)
       
               return ", ".join(cols_sql), cols_params
       
    1:     def relabeled_clone(self, relabels):
               return self.__class__(
                   relabels.get(self.alias, self.alias), self.targets, self.sources, self.field
               )
       
    1:     def resolve_expression(self, *args, **kwargs):
               return self
       
    1:     def select_format(self, compiler, sql, params):
               return sql, params
       
       
    2: class Ref(Expression):
    1:     """
           Reference to column alias of the query. For example, Ref('sum_cost') in
           qs.annotate(sum_cost=Sum('cost')) query.
           """
       
    1:     def __init__(self, refs, source):
  102:         super().__init__()
  102:         self.refs, self.source = refs, source
       
    1:     def __repr__(self):
               return "{}({}, {})".format(self.__class__.__name__, self.refs, self.source)
       
    1:     def get_source_expressions(self):
               return [self.source]
       
    1:     def set_source_expressions(self, exprs):
               (self.source,) = exprs
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               # The sub-expression `source` has already been resolved, as this is
               # just a reference to the name of `source`.
    8:         return self
       
    1:     def get_refs(self):
               return {self.refs}
       
    1:     def relabeled_clone(self, relabels):
               clone = self.copy()
               clone.source = self.source.relabeled_clone(relabels)
               return clone
       
    1:     def as_sql(self, compiler, connection):
               return connection.ops.quote_name(self.refs), []
       
    1:     def get_group_by_cols(self):
               return [self]
       
       
    2: class ExpressionList(Func):
    1:     """
           An expression containing multiple expressions. Can be used to provide a
           list of expressions as an argument to another expression, like a partition
           clause.
           """
       
    1:     template = "%(expressions)s"
       
    1:     def __str__(self):
               return self.arg_joiner.join(str(arg) for arg in self.source_expressions)
       
    1:     def as_sql(self, *args, **kwargs):
               if not self.source_expressions:
                   return "", ()
               return super().as_sql(*args, **kwargs)
       
    1:     def as_sqlite(self, compiler, connection, **extra_context):
               # Casting to numeric is unnecessary.
               return self.as_sql(compiler, connection, **extra_context)
       
    1:     def get_group_by_cols(self):
               group_by_cols = []
               for expr in self.get_source_expressions():
                   group_by_cols.extend(expr.get_group_by_cols())
               return group_by_cols
       
       
    2: class OrderByList(ExpressionList):
    1:     allowed_default = False
    1:     template = "ORDER BY %(expressions)s"
       
    1:     def __init__(self, *expressions, **extra):
               expressions = (
                   (
                       OrderBy(F(expr[1:]), descending=True)
                       if isinstance(expr, str) and expr[0] == "-"
                       else expr
                   )
                   for expr in expressions
               )
               super().__init__(*expressions, **extra)
       
       
    3: @deconstructible(path="django.db.models.ExpressionWrapper")
    2: class ExpressionWrapper(SQLiteNumericMixin, Expression):
    1:     """
           An expression that can wrap another expression so that it can provide
           extra context to the inner expression, such as the output_field.
           """
       
    1:     def __init__(self, expression, output_field):
               super().__init__(output_field=output_field)
               self.expression = expression
       
    1:     def set_source_expressions(self, exprs):
               self.expression = exprs[0]
       
    1:     def get_source_expressions(self):
               return [self.expression]
       
    1:     def get_group_by_cols(self):
               if isinstance(self.expression, Expression):
                   expression = self.expression.copy()
                   expression.output_field = self.output_field
                   return expression.get_group_by_cols()
               # For non-expressions e.g. an SQL WHERE clause, the entire
               # `expression` must be included in the GROUP BY clause.
               return super().get_group_by_cols()
       
    1:     def as_sql(self, compiler, connection):
               return compiler.compile(self.expression)
       
    1:     def __repr__(self):
               return "{}({})".format(self.__class__.__name__, self.expression)
       
    2:     @property
    2:     def allowed_default(self):
               return self.expression.allowed_default
       
       
    2: class NegatedExpression(ExpressionWrapper):
    1:     """The logical negation of a conditional expression."""
       
    1:     def __init__(self, expression):
               super().__init__(expression, output_field=fields.BooleanField())
       
    1:     def __invert__(self):
               return self.expression.copy()
       
    1:     def as_sql(self, compiler, connection):
               try:
                   sql, params = super().as_sql(compiler, connection)
               except EmptyResultSet:
                   features = compiler.connection.features
                   if not features.supports_boolean_expr_in_select_clause:
                       return "1=1", ()
                   return compiler.compile(Value(True))
               ops = compiler.connection.ops
               # Some database backends (e.g. Oracle) don't allow EXISTS() and filters
               # to be compared to another expression unless they're wrapped in a CASE
               # WHEN.
               if not ops.conditional_expression_supported_in_where_clause(self.expression):
                   return f"CASE WHEN {sql} = 0 THEN 1 ELSE 0 END", params
               return f"NOT {sql}", params
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               resolved = super().resolve_expression(
                   query, allow_joins, reuse, summarize, for_save
               )
               if not getattr(resolved.expression, "conditional", False):
                   raise TypeError("Cannot negate non-conditional expressions.")
               return resolved
       
    1:     def select_format(self, compiler, sql, params):
               # Wrap boolean expressions with a CASE WHEN expression if a database
               # backend (e.g. Oracle) doesn't support boolean expression in SELECT or
               # GROUP BY list.
               expression_supported_in_where_clause = (
                   compiler.connection.ops.conditional_expression_supported_in_where_clause
               )
               if (
                   not compiler.connection.features.supports_boolean_expr_in_select_clause
                   # Avoid double wrapping.
                   and expression_supported_in_where_clause(self.expression)
               ):
                   sql = "CASE WHEN {} THEN 1 ELSE 0 END".format(sql)
               return sql, params
       
       
    3: @deconstructible(path="django.db.models.When")
    2: class When(Expression):
    1:     template = "WHEN %(condition)s THEN %(result)s"
           # This isn't a complete conditional expression, must be used in Case().
    1:     conditional = False
       
    1:     def __init__(self, condition=None, then=None, **lookups):
               if lookups:
                   if condition is None:
                       condition, lookups = Q(**lookups), None
                   elif getattr(condition, "conditional", False):
                       condition, lookups = Q(condition, **lookups), None
               if condition is None or not getattr(condition, "conditional", False) or lookups:
                   raise TypeError(
                       "When() supports a Q object, a boolean expression, or lookups "
                       "as a condition."
                   )
               if isinstance(condition, Q) and not condition:
                   raise ValueError("An empty Q() can't be used as a When() condition.")
               super().__init__(output_field=None)
               self.condition = condition
               self.result = self._parse_expressions(then)[0]
       
    1:     def __str__(self):
               return "WHEN %r THEN %r" % (self.condition, self.result)
       
    1:     def __repr__(self):
               return "<%s: %s>" % (self.__class__.__name__, self)
       
    1:     def get_source_expressions(self):
               return [self.condition, self.result]
       
    1:     def set_source_expressions(self, exprs):
               self.condition, self.result = exprs
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               c = super().resolve_expression(query, allow_joins, reuse, summarize, for_save)
               if for_save and c.condition is not None:
                   # Resolve condition with for_save=False, since it's used as a filter.
                   c.condition = self.condition.resolve_expression(
                       query, allow_joins, reuse, summarize, for_save=False
                   )
               return c
       
    1:     def get_source_fields(self):
               # We're only interested in the fields of the result expressions.
               return [self.result._output_field_or_none]
       
    1:     def as_sql(self, compiler, connection, template=None, **extra_context):
               connection.ops.check_expression_support(self)
               template_params = extra_context
               sql_params = []
               condition_sql, condition_params = compiler.compile(self.condition)
               template_params["condition"] = condition_sql
               result_sql, result_params = compiler.compile(self.result)
               template_params["result"] = result_sql
               template = template or self.template
               return template % template_params, (
                   *sql_params,
                   *condition_params,
                   *result_params,
               )
       
    1:     def get_group_by_cols(self):
               # This is not a complete expression and cannot be used in GROUP BY.
               cols = []
               for source in self.get_source_expressions():
                   cols.extend(source.get_group_by_cols())
               return cols
       
    2:     @cached_property
    2:     def allowed_default(self):
               return self.condition.allowed_default and self.result.allowed_default
       
       
    3: @deconstructible(path="django.db.models.Case")
    2: class Case(SQLiteNumericMixin, Expression):
    1:     """
           An SQL searched CASE expression:
       
               CASE
                   WHEN n > 0
                       THEN 'positive'
                   WHEN n < 0
                       THEN 'negative'
                   ELSE 'zero'
               END
           """
       
    1:     template = "CASE %(cases)s ELSE %(default)s END"
    1:     case_joiner = " "
       
    1:     def __init__(self, *cases, default=None, output_field=None, **extra):
               if not all(isinstance(case, When) for case in cases):
                   raise TypeError("Positional arguments must all be When objects.")
               super().__init__(output_field)
               self.cases = list(cases)
               self.default = self._parse_expressions(default)[0]
               self.extra = extra
       
    1:     def __str__(self):
               return "CASE %s, ELSE %r" % (
                   ", ".join(str(c) for c in self.cases),
                   self.default,
               )
       
    1:     def __repr__(self):
               return "<%s: %s>" % (self.__class__.__name__, self)
       
    1:     def get_source_expressions(self):
               return self.cases + [self.default]
       
    1:     def set_source_expressions(self, exprs):
               *self.cases, self.default = exprs
       
    1:     def copy(self):
               c = super().copy()
               c.cases = c.cases[:]
               return c
       
    1:     def as_sql(
    1:         self, compiler, connection, template=None, case_joiner=None, **extra_context
           ):
               connection.ops.check_expression_support(self)
               if not self.cases:
                   return compiler.compile(self.default)
               template_params = {**self.extra, **extra_context}
               case_parts = []
               sql_params = []
               for case in self.cases:
                   try:
                       case_sql, case_params = compiler.compile(case)
                   except EmptyResultSet:
                       continue
                   except FullResultSet:
                       default_sql, default_params = compiler.compile(case.result)
                       break
                   case_parts.append(case_sql)
                   sql_params.extend(case_params)
               else:
                   default_sql, default_params = compiler.compile(self.default)
               if not case_parts:
                   return default_sql, default_params
               case_joiner = case_joiner or self.case_joiner
               template_params["cases"] = case_joiner.join(case_parts)
               template_params["default"] = default_sql
               sql_params.extend(default_params)
               template = template or template_params.get("template", self.template)
               sql = template % template_params
               if self._output_field_or_none is not None:
                   sql = connection.ops.unification_cast_sql(self.output_field) % sql
               return sql, sql_params
       
    1:     def get_group_by_cols(self):
               if not self.cases:
                   return self.default.get_group_by_cols()
               return super().get_group_by_cols()
       
    2:     @cached_property
    2:     def allowed_default(self):
               return self.default.allowed_default and all(
                   case_.allowed_default for case_ in self.cases
               )
       
       
    2: class Subquery(BaseExpression, Combinable):
    1:     """
           An explicit subquery. It may contain OuterRef() references to the outer
           query which will be resolved when it is applied to that query.
           """
       
    1:     template = "(%(subquery)s)"
    1:     contains_aggregate = False
    1:     empty_result_set_value = None
    1:     subquery = True
       
    1:     def __init__(self, queryset, output_field=None, **extra):
               # Allow the usage of both QuerySet and sql.Query objects.
               self.query = getattr(queryset, "query", queryset).clone()
               self.query.subquery = True
               self.extra = extra
               super().__init__(output_field)
       
    1:     def get_source_expressions(self):
               return [self.query]
       
    1:     def set_source_expressions(self, exprs):
               self.query = exprs[0]
       
    1:     def _resolve_output_field(self):
               return self.query.output_field
       
    1:     def copy(self):
               clone = super().copy()
               clone.query = clone.query.clone()
               return clone
       
    2:     @property
    2:     def external_aliases(self):
               return self.query.external_aliases
       
    1:     def get_external_cols(self):
               return self.query.get_external_cols()
       
    1:     def as_sql(self, compiler, connection, template=None, **extra_context):
               connection.ops.check_expression_support(self)
               template_params = {**self.extra, **extra_context}
               subquery_sql, sql_params = self.query.as_sql(compiler, connection)
               template_params["subquery"] = subquery_sql[1:-1]
       
               template = template or template_params.get("template", self.template)
               sql = template % template_params
               return sql, sql_params
       
    1:     def get_group_by_cols(self):
               return self.query.get_group_by_cols(wrapper=self)
       
       
    2: class Exists(Subquery):
    1:     template = "EXISTS(%(subquery)s)"
    1:     output_field = fields.BooleanField()
    1:     empty_result_set_value = False
       
    1:     def __init__(self, queryset, **kwargs):
               super().__init__(queryset, **kwargs)
               self.query = self.query.exists()
       
    1:     def select_format(self, compiler, sql, params):
               # Wrap EXISTS() with a CASE WHEN expression if a database backend
               # (e.g. Oracle) doesn't support boolean expression in SELECT or GROUP
               # BY list.
               if not compiler.connection.features.supports_boolean_expr_in_select_clause:
                   sql = "CASE WHEN {} THEN 1 ELSE 0 END".format(sql)
               return sql, params
       
    1:     def as_sql(self, compiler, *args, **kwargs):
               try:
                   return super().as_sql(compiler, *args, **kwargs)
               except EmptyResultSet:
                   features = compiler.connection.features
                   if not features.supports_boolean_expr_in_select_clause:
                       return "1=0", ()
                   return compiler.compile(Value(False))
       
       
    3: @deconstructible(path="django.db.models.OrderBy")
    2: class OrderBy(Expression):
    1:     template = "%(expression)s %(ordering)s"
    1:     conditional = False
    1:     constraint_validation_compatible = False
    1:     allows_composite_expressions = True
       
    1:     def __init__(self, expression, descending=False, nulls_first=None, nulls_last=None):
   40:         if nulls_first and nulls_last:
                   raise ValueError("nulls_first and nulls_last are mutually exclusive")
   40:         if nulls_first is False or nulls_last is False:
                   raise ValueError("nulls_first and nulls_last values must be True or None.")
   40:         self.nulls_first = nulls_first
   40:         self.nulls_last = nulls_last
   40:         self.descending = descending
   40:         if not hasattr(expression, "resolve_expression"):
                   raise ValueError("expression must be an expression type")
   40:         self.expression = expression
       
    1:     def __repr__(self):
               return "{}({}, descending={})".format(
                   self.__class__.__name__, self.expression, self.descending
               )
       
    1:     def set_source_expressions(self, exprs):
   40:         self.expression = exprs[0]
       
    1:     def get_source_expressions(self):
   40:         return [self.expression]
       
    1:     def as_sql(self, compiler, connection, template=None, **extra_context):
   40:         if isinstance(self.expression, ColPairs):
                   sql_parts = []
                   params = []
                   for col in self.expression.get_cols():
                       copy = self.copy()
                       copy.set_source_expressions([col])
                       sql, col_params = compiler.compile(copy)
                       sql_parts.append(sql)
                       params.extend(col_params)
                   return ", ".join(sql_parts), params
   40:         template = template or self.template
   40:         if connection.features.supports_order_by_nulls_modifier:
   40:             if self.nulls_last:
                       template = "%s NULLS LAST" % template
   40:             elif self.nulls_first:
                       template = "%s NULLS FIRST" % template
               else:
                   if self.nulls_last and not (
                       self.descending and connection.features.order_by_nulls_first
                   ):
                       template = "%%(expression)s IS NULL, %s" % template
                   elif self.nulls_first and not (
                       not self.descending and connection.features.order_by_nulls_first
                   ):
                       template = "%%(expression)s IS NOT NULL, %s" % template
   40:         connection.ops.check_expression_support(self)
   40:         expression_sql, params = compiler.compile(self.expression)
   80:         placeholders = {
   40:             "expression": expression_sql,
   40:             "ordering": "DESC" if self.descending else "ASC",
   40:             **extra_context,
               }
   40:         params *= template.count("%(expression)s")
   40:         return (template % placeholders).rstrip(), params
       
    1:     def as_oracle(self, compiler, connection):
               # Oracle < 23c doesn't allow ORDER BY EXISTS() or filters unless it's
               # wrapped in a CASE WHEN.
               if (
                   not connection.features.supports_boolean_expr_in_select_clause
                   and connection.ops.conditional_expression_supported_in_where_clause(
                       self.expression
                   )
               ):
                   copy = self.copy()
                   copy.expression = Case(
                       When(self.expression, then=True),
                       default=False,
                   )
                   return copy.as_sql(compiler, connection)
               return self.as_sql(compiler, connection)
       
    1:     def get_group_by_cols(self):
               cols = []
               for source in self.get_source_expressions():
                   cols.extend(source.get_group_by_cols())
               return cols
       
    1:     def reverse_ordering(self):
               self.descending = not self.descending
               if self.nulls_first:
                   self.nulls_last = True
                   self.nulls_first = None
               elif self.nulls_last:
                   self.nulls_first = True
                   self.nulls_last = None
               return self
       
    1:     def asc(self):
               self.descending = False
       
    1:     def desc(self):
               self.descending = True
       
       
    2: class Window(SQLiteNumericMixin, Expression):
    1:     template = "%(expression)s OVER (%(window)s)"
           # Although the main expression may either be an aggregate or an
           # expression with an aggregate function, the GROUP BY that will
           # be introduced in the query as a result is not desired.
    1:     contains_aggregate = False
    1:     contains_over_clause = True
       
    1:     def __init__(
               self,
               expression,
    1:         partition_by=None,
    1:         order_by=None,
    1:         frame=None,
    1:         output_field=None,
           ):
               self.partition_by = partition_by
               self.order_by = order_by
               self.frame = frame
       
               if not getattr(expression, "window_compatible", False):
                   raise ValueError(
                       "Expression '%s' isn't compatible with OVER clauses."
                       % expression.__class__.__name__
                   )
       
               if self.partition_by is not None:
                   if not isinstance(self.partition_by, (tuple, list)):
                       self.partition_by = (self.partition_by,)
                   self.partition_by = ExpressionList(*self.partition_by)
       
               if self.order_by is not None:
                   if isinstance(self.order_by, (list, tuple)):
                       self.order_by = OrderByList(*self.order_by)
                   elif isinstance(self.order_by, (BaseExpression, str)):
                       self.order_by = OrderByList(self.order_by)
                   else:
                       raise ValueError(
                           "Window.order_by must be either a string reference to a "
                           "field, an expression, or a list or tuple of them."
                       )
               super().__init__(output_field=output_field)
               self.source_expression = self._parse_expressions(expression)[0]
       
    1:     def _resolve_output_field(self):
               return self.source_expression.output_field
       
    1:     def get_source_expressions(self):
               return [self.source_expression, self.partition_by, self.order_by, self.frame]
       
    1:     def set_source_expressions(self, exprs):
               self.source_expression, self.partition_by, self.order_by, self.frame = exprs
       
    1:     def as_sql(self, compiler, connection, template=None):
               connection.ops.check_expression_support(self)
               if not connection.features.supports_over_clause:
                   raise NotSupportedError("This backend does not support window expressions.")
               expr_sql, params = compiler.compile(self.source_expression)
               window_sql, window_params = [], ()
       
               if self.partition_by is not None:
                   sql_expr, sql_params = self.partition_by.as_sql(
                       compiler=compiler,
                       connection=connection,
                       template="PARTITION BY %(expressions)s",
                   )
                   window_sql.append(sql_expr)
                   window_params += tuple(sql_params)
       
               if self.order_by is not None:
                   order_sql, order_params = compiler.compile(self.order_by)
                   window_sql.append(order_sql)
                   window_params += tuple(order_params)
       
               if self.frame:
                   frame_sql, frame_params = compiler.compile(self.frame)
                   window_sql.append(frame_sql)
                   window_params += tuple(frame_params)
       
               template = template or self.template
       
               return (
                   template % {"expression": expr_sql, "window": " ".join(window_sql).strip()},
                   (*params, *window_params),
               )
       
    1:     def as_sqlite(self, compiler, connection):
               if isinstance(self.output_field, fields.DecimalField):
                   # Casting to numeric must be outside of the window expression.
                   copy = self.copy()
                   source_expressions = copy.get_source_expressions()
                   source_expressions[0].output_field = fields.FloatField()
                   copy.set_source_expressions(source_expressions)
                   return super(Window, copy).as_sqlite(compiler, connection)
               return self.as_sql(compiler, connection)
       
    1:     def __str__(self):
               return "{} OVER ({}{}{})".format(
                   str(self.source_expression),
                   "PARTITION BY " + str(self.partition_by) if self.partition_by else "",
                   str(self.order_by or ""),
                   str(self.frame or ""),
               )
       
    1:     def __repr__(self):
               return "<%s: %s>" % (self.__class__.__name__, self)
       
    1:     def get_group_by_cols(self):
               group_by_cols = []
               if self.partition_by:
                   group_by_cols.extend(self.partition_by.get_group_by_cols())
               if self.order_by is not None:
                   group_by_cols.extend(self.order_by.get_group_by_cols())
               return group_by_cols
       
       
    2: class WindowFrameExclusion(Enum):
    1:     CURRENT_ROW = "CURRENT ROW"
    1:     GROUP = "GROUP"
    1:     TIES = "TIES"
    1:     NO_OTHERS = "NO OTHERS"
       
    1:     def __repr__(self):
               return f"{self.__class__.__qualname__}.{self._name_}"
       
       
    2: class WindowFrame(Expression):
    1:     """
           Model the frame clause in window expressions. There are two types of frame
           clauses which are subclasses, however, all processing and validation (by no
           means intended to be complete) is done here. Thus, providing an end for a
           frame is optional (the default is UNBOUNDED FOLLOWING, which is the last
           row in the frame).
           """
       
    1:     template = "%(frame_type)s BETWEEN %(start)s AND %(end)s%(exclude)s"
       
    1:     def __init__(self, start=None, end=None, exclusion=None):
               self.start = Value(start)
               self.end = Value(end)
               if not isinstance(exclusion, (NoneType, WindowFrameExclusion)):
                   raise TypeError(
                       f"{self.__class__.__qualname__}.exclusion must be a "
                       "WindowFrameExclusion instance."
                   )
               self.exclusion = exclusion
       
    1:     def set_source_expressions(self, exprs):
               self.start, self.end = exprs
       
    1:     def get_source_expressions(self):
               return [self.start, self.end]
       
    1:     def get_exclusion(self):
               if self.exclusion is None:
                   return ""
               return f" EXCLUDE {self.exclusion.value}"
       
    1:     def as_sql(self, compiler, connection):
               connection.ops.check_expression_support(self)
               start, end = self.window_frame_start_end(
                   connection, self.start.value, self.end.value
               )
               if self.exclusion and not connection.features.supports_frame_exclusion:
                   raise NotSupportedError(
                       "This backend does not support window frame exclusions."
                   )
               return (
                   self.template
                   % {
                       "frame_type": self.frame_type,
                       "start": start,
                       "end": end,
                       "exclude": self.get_exclusion(),
                   },
                   [],
               )
       
    1:     def __repr__(self):
               return "<%s: %s>" % (self.__class__.__name__, self)
       
    1:     def get_group_by_cols(self):
               return []
       
    1:     def __str__(self):
               if self.start.value is not None and self.start.value < 0:
                   start = "%d %s" % (abs(self.start.value), connection.ops.PRECEDING)
               elif self.start.value is not None and self.start.value == 0:
                   start = connection.ops.CURRENT_ROW
               elif self.start.value is not None and self.start.value > 0:
                   start = "%d %s" % (self.start.value, connection.ops.FOLLOWING)
               else:
                   start = connection.ops.UNBOUNDED_PRECEDING
       
               if self.end.value is not None and self.end.value > 0:
                   end = "%d %s" % (self.end.value, connection.ops.FOLLOWING)
               elif self.end.value is not None and self.end.value == 0:
                   end = connection.ops.CURRENT_ROW
               elif self.end.value is not None and self.end.value < 0:
                   end = "%d %s" % (abs(self.end.value), connection.ops.PRECEDING)
               else:
                   end = connection.ops.UNBOUNDED_FOLLOWING
               return self.template % {
                   "frame_type": self.frame_type,
                   "start": start,
                   "end": end,
                   "exclude": self.get_exclusion(),
               }
       
    1:     def window_frame_start_end(self, connection, start, end):
               raise NotImplementedError("Subclasses must implement window_frame_start_end().")
       
       
    2: class RowRange(WindowFrame):
    1:     frame_type = "ROWS"
       
    1:     def window_frame_start_end(self, connection, start, end):
               return connection.ops.window_frame_rows_start_end(start, end)
       
       
    2: class ValueRange(WindowFrame):
    1:     frame_type = "RANGE"
       
    1:     def window_frame_start_end(self, connection, start, end):
               return connection.ops.window_frame_range_start_end(start, end)
