    1: import bisect
    1: import copy
    1: from collections import defaultdict
       
    1: from django.apps import apps
    1: from django.conf import settings
    1: from django.core.exceptions import FieldDoesNotExist, ImproperlyConfigured
    1: from django.core.signals import setting_changed
    1: from django.db import connections
    1: from django.db.models import (
           AutoField,
           CompositePrimaryKey,
           Manager,
           OrderWrt,
           UniqueConstraint,
       )
    1: from django.db.models.fields import composite
    1: from django.db.models.query_utils import PathInfo
    1: from django.utils.datastructures import ImmutableList, OrderedSet
    1: from django.utils.functional import cached_property
    1: from django.utils.module_loading import import_string
    1: from django.utils.text import camel_case_to_spaces, format_lazy
    1: from django.utils.translation import override
       
    1: PROXY_PARENTS = object()
       
    1: EMPTY_RELATION_TREE = ()
       
    1: IMMUTABLE_WARNING = (
    1:     "The return type of '%s' should never be mutated. If you want to manipulate this "
           "list for your own use, make a copy first."
       )
       
    1: DEFAULT_NAMES = (
           "verbose_name",
           "verbose_name_plural",
           "db_table",
           "db_table_comment",
           "ordering",
           "unique_together",
           "permissions",
           "get_latest_by",
           "order_with_respect_to",
           "app_label",
           "db_tablespace",
           "abstract",
           "managed",
           "proxy",
           "swappable",
           "auto_created",
           "apps",
           "default_permissions",
           "select_on_save",
           "default_related_name",
           "required_db_features",
           "required_db_vendor",
           "base_manager_name",
           "default_manager_name",
           "indexes",
           "constraints",
       )
       
       
    1: def normalize_together(option_together):
           """
           option_together can be either a tuple of tuples, or a single
           tuple of two strings. Normalize it to a tuple of tuples, so that
           calling code can uniformly expect that.
           """
  348:     try:
  348:         if not option_together:
  206:             return ()
  142:         if not isinstance(option_together, (tuple, list)):
   60:             raise TypeError
   82:         first_element = option_together[0]
   82:         if not isinstance(first_element, (tuple, list)):
   76:             option_together = (option_together,)
               # Normalize everything to tuples
  246:         return tuple(tuple(ot) for ot in option_together)
   60:     except TypeError:
               # If the value of option_together isn't valid, return it
               # verbatim; this will be picked up by the check framework later.
   60:         return option_together
       
       
    1: def make_immutable_fields_list(name, data):
 1907:     return ImmutableList(data, warning=IMMUTABLE_WARNING % name)
       
       
    2: class Options:
    1:     FORWARD_PROPERTIES = {
               "fields",
               "many_to_many",
               "concrete_fields",
               "local_concrete_fields",
               "_non_pk_concrete_field_names",
               "_reverse_one_to_one_field_names",
               "_forward_fields_map",
               "managers",
               "managers_map",
               "base_manager",
               "default_manager",
           }
    1:     REVERSE_PROPERTIES = {"related_objects", "fields_map", "_relation_tree"}
       
    1:     default_apps = apps
       
    1:     def __init__(self, meta, app_label=None):
  354:         self._get_fields_cache = {}
  354:         self.local_fields = []
  354:         self.local_many_to_many = []
  354:         self.private_fields = []
  354:         self.local_managers = []
  354:         self.base_manager_name = None
  354:         self.default_manager_name = None
  354:         self.model_name = None
  354:         self.verbose_name = None
  354:         self.verbose_name_plural = None
  354:         self.db_table = ""
  354:         self.db_table_comment = ""
  354:         self.ordering = []
  354:         self._ordering_clash = False
  354:         self.indexes = []
  354:         self.constraints = []
  354:         self.unique_together = []
  354:         self.select_on_save = False
  354:         self.default_permissions = ("add", "change", "delete", "view")
  354:         self.permissions = []
  354:         self.object_name = None
  354:         self.app_label = app_label
  354:         self.get_latest_by = None
  354:         self.order_with_respect_to = None
  354:         self.db_tablespace = settings.DEFAULT_TABLESPACE
  354:         self.required_db_features = []
  354:         self.required_db_vendor = None
  354:         self.meta = meta
  354:         self.pk = None
  354:         self.auto_field = None
  354:         self.abstract = False
  354:         self.managed = True
  354:         self.proxy = False
               # For any class that is a proxy (including automatically created
               # classes for deferred object loading), proxy_for_model tells us
               # which class this model is proxying. Note that proxy_for_model
               # can create a chain of proxy models. For non-proxy models, the
               # variable is always None.
  354:         self.proxy_for_model = None
               # For any non-abstract class, the concrete class is the model
               # in the end of the proxy_for_model chain. In particular, for
               # concrete models, the concrete_model is always the class itself.
  354:         self.concrete_model = None
  354:         self.swappable = None
  354:         self.parents = {}
  354:         self.auto_created = False
       
               # List of all lookups defined in ForeignKey 'limit_choices_to' options
               # from *other* models. Needed for some admin checks. Internal use only.
  354:         self.related_fkey_lookups = []
       
               # A custom app registry to use, if you're making a separate model set.
  354:         self.apps = self.default_apps
       
  354:         self.default_related_name = None
       
    2:     @property
    2:     def label(self):
 1097:         return "%s.%s" % (self.app_label, self.object_name)
       
    2:     @property
    2:     def label_lower(self):
  628:         return "%s.%s" % (self.app_label, self.model_name)
       
    2:     @property
    2:     def app_config(self):
               # Don't go through get_app_config to avoid triggering imports.
  311:         return self.apps.app_configs.get(self.app_label)
       
    1:     def contribute_to_class(self, cls, name):
  354:         from django.db import connection
  354:         from django.db.backends.utils import truncate_name
       
  354:         cls._meta = self
  354:         self.model = cls
               # First, construct the default values for these options.
  354:         self.object_name = cls.__name__
  354:         self.model_name = self.object_name.lower()
  354:         self.verbose_name = camel_case_to_spaces(self.object_name)
       
               # Store the original user-defined values for each option,
               # for use when serializing the model definition
  354:         self.original_attrs = {}
       
               # Next, apply any overridden values from 'class Meta'.
  354:         if self.meta:
  347:             meta_attrs = self.meta.__dict__.copy()
 3894:             for name in self.meta.__dict__:
                       # Ignore any private attributes that Django doesn't care about.
                       # NOTE: We can't modify a dictionary's contents while looping
                       # over it, so we loop over the *original* dictionary instead.
 3547:                 if name.startswith("_"):
 1384:                     del meta_attrs[name]
 9369:             for attr_name in DEFAULT_NAMES:
 9022:                 if attr_name in meta_attrs:
 2163:                     setattr(self, attr_name, meta_attrs.pop(attr_name))
 2163:                     self.original_attrs[attr_name] = getattr(self, attr_name)
 6859:                 elif hasattr(self.meta, attr_name):
    6:                     setattr(self, attr_name, getattr(self.meta, attr_name))
    6:                     self.original_attrs[attr_name] = getattr(self, attr_name)
       
  347:             self.unique_together = normalize_together(self.unique_together)
                   # App label/class name interpolation for names of constraints and
                   # indexes.
  347:             if not self.abstract:
  343:                 self.constraints = self._format_names(self.constraints)
  343:                 self.indexes = self._format_names(self.indexes)
       
                   # verbose_name_plural is a special case because it uses a 's'
                   # by default.
  347:             if self.verbose_name_plural is None:
  131:                 self.verbose_name_plural = format_lazy("{}s", self.verbose_name)
       
                   # order_with_respect_and ordering are mutually exclusive.
  347:             self._ordering_clash = bool(self.ordering and self.order_with_respect_to)
       
                   # Any leftover attributes must be invalid.
  347:             if meta_attrs != {}:
                       raise TypeError(
                           "'class Meta' got invalid attribute(s): %s" % ",".join(meta_attrs)
                       )
               else:
    7:             self.verbose_name_plural = format_lazy("{}s", self.verbose_name)
  354:         del self.meta
       
               # If the db_table wasn't provided, use the app_label + model_name.
  354:         if not self.db_table:
  200:             self.db_table = "%s_%s" % (self.app_label, self.model_name)
  400:             self.db_table = truncate_name(
  200:                 self.db_table, connection.ops.max_name_length()
                   )
       
  354:         if self.swappable:
   25:             setting_changed.connect(self.setting_changed)
       
    1:     def _format_names(self, objs):
               """App label/class name interpolation for object names."""
  686:         names = {"app_label": self.app_label.lower(), "class": self.model_name}
  686:         new_objs = []
  733:         for obj in objs:
   47:             obj = obj.clone()
   47:             obj.name %= names
   47:             new_objs.append(obj)
  686:         return new_objs
       
    1:     def _get_default_pk_class(self):
  190:         pk_class_path = getattr(
   95:             self.app_config,
   95:             "default_auto_field",
   95:             settings.DEFAULT_AUTO_FIELD,
               )
   95:         if self.app_config and self.app_config._is_default_auto_field_overridden:
   22:             app_config_class = type(self.app_config)
   22:             source = (
   44:                 f"{app_config_class.__module__}."
   22:                 f"{app_config_class.__qualname__}.default_auto_field"
                   )
               else:
   73:             source = "DEFAULT_AUTO_FIELD"
   95:         if not pk_class_path:
                   raise ImproperlyConfigured(f"{source} must not be empty.")
   95:         try:
   95:             pk_class = import_string(pk_class_path)
               except ImportError as e:
                   msg = (
                       f"{source} refers to the module '{pk_class_path}' that could "
                       f"not be imported."
                   )
                   raise ImproperlyConfigured(msg) from e
   95:         if not issubclass(pk_class, AutoField):
                   raise ValueError(
                       f"Primary key '{pk_class_path}' referred by {source} must "
                       f"subclass AutoField."
                   )
   95:         return pk_class
       
    1:     def _prepare(self, model):
  350:         if self.order_with_respect_to:
                   # The app registry will not be ready at this point, so we cannot
                   # use get_field().
                   query = self.order_with_respect_to
                   try:
                       self.order_with_respect_to = next(
                           f
                           for f in self._get_fields(reverse=False)
                           if f.name == query or f.attname == query
                       )
                   except StopIteration:
                       raise FieldDoesNotExist(
                           "%s has no field named '%s'" % (self.object_name, query)
                       )
       
                   self.ordering = ("_order",)
                   if not any(
                       isinstance(field, OrderWrt) for field in model._meta.local_fields
                   ):
                       model.add_to_class("_order", OrderWrt())
               else:
  350:             self.order_with_respect_to = None
       
  350:         if self.pk is None:
   95:             if self.parents:
                       # Promote the first parent link in lieu of adding yet another
                       # field.
                       field = next(iter(self.parents.values()))
                       # Look for a local field with the same name as the
                       # first parent link. If a local field has already been
                       # created, use it instead of promoting the parent
                       already_created = [
                           fld for fld in self.local_fields if fld.name == field.name
                       ]
                       if already_created:
                           field = already_created[0]
                       field.primary_key = True
                       self.setup_pk(field)
                   else:
   95:                 pk_class = self._get_default_pk_class()
   95:                 auto = pk_class(verbose_name="ID", primary_key=True, auto_created=True)
   95:                 model.add_to_class("id", auto)
       
    1:     def add_manager(self, manager):
  351:         self.local_managers.append(manager)
  351:         self._expire_cache()
       
    1:     def add_field(self, field, private=False):
               # Insert the given field in the order in which it was created, using
               # the "creation_counter" attribute of the field.
               # Move many-to-many related fields from self.fields into
               # self.many_to_many.
 1987:         if private:
                   self.private_fields.append(field)
 1987:         elif field.is_relation and field.many_to_many:
   79:             bisect.insort(self.local_many_to_many, field)
               else:
 1908:             bisect.insort(self.local_fields, field)
 1908:             self.setup_pk(field)
       
               # If the field being added is a relation to another known field,
               # expire the cache on this field and the forward cache on the field
               # being referenced, because there will be new relationships in the
               # cache. Otherwise, expire the cache of references *to* this field.
               # The mechanism for getting at the related model is slightly odd -
               # ideally, we'd just ask for field.related_model. However, related_model
               # is a cached property, and all the models haven't been loaded yet, so
               # we need to make sure we don't cache a string reference.
 2857:         if (
 1987:             field.is_relation
  435:             and hasattr(field.remote_field, "model")
  435:             and field.remote_field.model
               ):
  435:             try:
  435:                 field.remote_field.model._meta._expire_cache(forward=False)
  327:             except AttributeError:
  327:                 pass
  435:             self._expire_cache()
               else:
 1552:             self._expire_cache(reverse=False)
       
    1:     def setup_pk(self, field):
 1908:         if not self.pk and field.primary_key:
  351:             self.pk = field
  351:             field.serialize = False
       
    1:     def setup_proxy(self, target):
               """
               Do the internal setup so that the current model is a proxy for
               "target".
               """
               self.pk = target._meta.pk
               self.proxy_for_model = target
               self.db_table = target._meta.db_table
       
    1:     def __repr__(self):
               return "<Options for %s>" % self.object_name
       
    1:     def __str__(self):
               return self.label_lower
       
    1:     def can_migrate(self, connection):
               """
               Return True if the model can/should be migrated on the `connection`.
               `connection` can be either a real connection or a connection alias.
               """
   39:         if self.proxy or self.swapped or not self.managed:
                   return False
   39:         if isinstance(connection, str):
   39:             connection = connections[connection]
   39:         if self.required_db_vendor:
                   return self.required_db_vendor == connection.vendor
   39:         if self.required_db_features:
                   return all(
                       getattr(connection.features, feat, False)
                       for feat in self.required_db_features
                   )
   39:         return True
       
    2:     @cached_property
    2:     def verbose_name_raw(self):
               """Return the untranslated verbose name."""
   40:         if isinstance(self.verbose_name, str):
   34:             return self.verbose_name
   12:         with override(None):
    6:             return str(self.verbose_name)
       
    2:     @cached_property
    2:     def swapped(self):
               """
               Has this model been swapped out for another? If so, return the model
               name of the replacement; otherwise, return None.
       
               For historical reasons, model name lookups using get_model() are
               case insensitive, so we make sure we are case insensitive here.
               """
  341:         if self.swappable:
   25:             swapped_for = getattr(settings, self.swappable, None)
   25:             if swapped_for:
   25:                 try:
   25:                     swapped_label, swapped_object = swapped_for.split(".")
                       except ValueError:
                           # setting not in the format app_label.model_name
                           # raising ImproperlyConfigured here causes problems with
                           # test cleanup code - instead it is raised in get_user_model
                           # or as part of validation.
                           return swapped_for
       
                       if (
   50:                     "%s.%s" % (swapped_label, swapped_object.lower())
   25:                     != self.label_lower
                       ):
                           return swapped_for
  341:         return None
       
    1:     def setting_changed(self, *, setting, **kwargs):
   16:         if setting == self.swappable and "swapped" in self.__dict__:
                   del self.swapped
       
    2:     @cached_property
    2:     def managers(self):
  367:         managers = []
  367:         seen_managers = set()
 2206:         bases = (b for b in self.model.mro() if hasattr(b, "_meta"))
  738:         for depth, base in enumerate(bases):
  528:             for manager in base._meta.local_managers:
  157:                 if manager.name in seen_managers:
    1:                     continue
       
  156:                 manager = copy.copy(manager)
  156:                 manager.model = self.model
  156:                 seen_managers.add(manager.name)
  156:                 managers.append((depth, manager.creation_counter, manager))
       
  734:         return make_immutable_fields_list(
  367:             "managers",
  890:             (m[2] for m in sorted(managers)),
               )
       
    2:     @cached_property
    2:     def managers_map(self):
   54:         return {manager.name: manager for manager in self.managers}
       
    2:     @cached_property
    2:     def base_manager(self):
   19:         base_manager_name = self.base_manager_name
   19:         if not base_manager_name:
                   # Get the first parent's base_manager_name if there's one.
   67:             for parent in self.model.mro()[1:]:
   51:                 if hasattr(parent, "_meta"):
    3:                     if parent._base_manager.name != "_base_manager":
                               base_manager_name = parent._base_manager.name
    3:                     break
       
   19:         if base_manager_name:
                   try:
                       return self.managers_map[base_manager_name]
                   except KeyError:
                       raise ValueError(
                           "%s has no manager named %r"
                           % (
                               self.object_name,
                               base_manager_name,
                           )
                       )
       
   19:         manager = Manager()
   19:         manager.name = "_base_manager"
   19:         manager.model = self.model
   19:         manager.auto_created = True
   19:         return manager
       
    2:     @cached_property
    2:     def default_manager(self):
    8:         default_manager_name = self.default_manager_name
    8:         if not default_manager_name and not self.local_managers:
                   # Get the first parent's default_manager_name if there's one.
    1:             for parent in self.model.mro()[1:]:
    1:                 if hasattr(parent, "_meta"):
    1:                     default_manager_name = parent._meta.default_manager_name
    1:                     break
       
    8:         if default_manager_name:
                   try:
                       return self.managers_map[default_manager_name]
                   except KeyError:
                       raise ValueError(
                           "%s has no manager named %r"
                           % (
                               self.object_name,
                               default_manager_name,
                           )
                       )
       
    8:         if self.managers:
    8:             return self.managers[0]
       
    2:     @cached_property
    2:     def fields(self):
               """
               Return a list of all forward fields on the model and its parents,
               excluding ManyToManyFields.
       
               Private API intended only to be used by Django itself; get_fields()
               combined with filtering of field properties is the public API for
               obtaining this field list.
               """
       
               # For legacy reasons, the fields property should only contain forward
               # fields that are not private or with a m2m cardinality. Therefore we
               # pass these three filters as filters to the generator.
               # The third lambda is a longwinded way of checking f.related_model - we don't
               # use that property directly because related_model is a cached property,
               # and all the models may not have been loaded yet; we don't want to cache
               # the string reference to the related_model.
  370:         def is_not_an_m2m_field(f):
 2076:             return not (f.is_relation and f.many_to_many)
       
  370:         def is_not_a_generic_relation(f):
 2001:             return not (f.is_relation and f.one_to_many)
       
  370:         def is_not_a_generic_foreign_key(f):
 2001:             return not (
 2380:                 f.is_relation
  379:                 and f.many_to_one
  360:                 and not (hasattr(f.remote_field, "model") and f.remote_field.model)
                   )
       
  740:         return make_immutable_fields_list(
  370:             "fields",
11265:             (
 2001:                 f
 2446:                 for f in self._get_fields(reverse=False)
 2076:                 if is_not_an_m2m_field(f)
 2001:                 and is_not_a_generic_relation(f)
 2001:                 and is_not_a_generic_foreign_key(f)
                   ),
               )
       
    2:     @cached_property
    2:     def concrete_fields(self):
               """
               Return a list of all concrete fields on the model and its parents.
       
               Private API intended only to be used by Django itself; get_fields()
               combined with filtering of field properties is the public API for
               obtaining this field list.
               """
   38:         return make_immutable_fields_list(
  152:             "concrete_fields", (f for f in self.fields if f.concrete)
               )
       
    2:     @cached_property
    2:     def local_concrete_fields(self):
               """
               Return a list of all concrete fields on the model.
       
               Private API intended only to be used by Django itself; get_fields()
               combined with filtering of field properties is the public API for
               obtaining this field list.
               """
   50:         return make_immutable_fields_list(
  225:             "local_concrete_fields", (f for f in self.local_fields if f.concrete)
               )
       
    2:     @cached_property
    2:     def many_to_many(self):
               """
               Return a list of all many to many fields on the model and its parents.
       
               Private API intended only to be used by Django itself; get_fields()
               combined with filtering of field properties is the public API for
               obtaining this list.
               """
   26:         return make_immutable_fields_list(
   13:             "many_to_many",
  228:             (
    3:                 f
   99:                 for f in self._get_fields(reverse=False)
  100:                 if f.is_relation and f.many_to_many
                   ),
               )
       
    2:     @cached_property
    2:     def related_objects(self):
               """
               Return all related objects pointing to the current model. The related
               objects can come from a one-to-one, one-to-many, or many-to-many field
               relation type.
       
               Private API intended only to be used by Django itself; get_fields()
               combined with filtering of field properties is the public API for
               obtaining this field list.
               """
   44:         all_related_fields = self._get_fields(
   22:             forward=False, reverse=True, include_hidden=True
               )
   44:         return make_immutable_fields_list(
   22:             "related_objects",
  168:             (
   24:                 obj
   56:                 for obj in all_related_fields
   44:                 if not obj.hidden or obj.field.many_to_many
                   ),
               )
       
    2:     @cached_property
    2:     def _forward_fields_map(self):
   81:         res = {}
   81:         fields = self._get_fields(reverse=False)
  656:         for field in fields:
  575:             res[field.name] = field
                   # Due to the way Django's internals work, get_field() should also
                   # be able to fetch a field by attname. In the case of a concrete
                   # field with relation, includes the *_id name too
  575:             try:
  575:                 res[field.attname] = field
                   except AttributeError:
                       pass
   81:         return res
       
    2:     @cached_property
    2:     def fields_map(self):
    4:         res = {}
    4:         fields = self._get_fields(forward=False, include_hidden=True)
   13:         for field in fields:
    9:             res[field.name] = field
                   # Due to the way Django's internals work, get_field() should also
                   # be able to fetch a field by attname. In the case of a concrete
                   # field with relation, includes the *_id name too
    9:             try:
    9:                 res[field.attname] = field
    9:             except AttributeError:
    9:                 pass
    4:         return res
       
    1:     def get_field(self, field_name):
               """
               Return a field instance given the name of a forward or reverse field.
               """
 1527:         try:
                   # In order to avoid premature loading of the relation tree
                   # (expensive) we prefer checking if the field is a forward field.
 1527:             return self._forward_fields_map[field_name]
   68:         except KeyError:
                   # If the app registry is not ready, reverse fields are
                   # unavailable, therefore we throw a FieldDoesNotExist exception.
   68:             if not self.apps.models_ready:
                       raise FieldDoesNotExist(
                           "%s has no field named '%s'. The app cache isn't ready yet, "
                           "so if this is an auto-created related field, it won't "
                           "be available yet." % (self.object_name, field_name)
                       )
       
   68:         try:
                   # Retrieve field instance by name from cached or just-computed
                   # field map.
   68:             return self.fields_map[field_name]
   32:         except KeyError:
   64:             raise FieldDoesNotExist(
   32:                 "%s has no field named '%s'" % (self.object_name, field_name)
                   )
       
    1:     def get_base_chain(self, model):
               """
               Return a list of parent classes leading to `model` (ordered from
               closest to most distant ancestor). This has to handle the case where
               `model` is a grandparent or even more distant relation.
               """
               if not self.parents:
                   return []
               if model in self.parents:
                   return [model]
               for parent in self.parents:
                   res = parent._meta.get_base_chain(model)
                   if res:
                       res.insert(0, parent)
                       return res
               return []
       
    2:     @cached_property
    2:     def all_parents(self):
               """
               Return all the ancestors of this model as a tuple ordered by MRO.
               Useful for determining if something is an ancestor, regardless of lineage.
               """
   22:         result = OrderedSet(self.parents)
   22:         for parent in self.parents:
                   for ancestor in parent._meta.all_parents:
                       result.add(ancestor)
   22:         return tuple(result)
       
    1:     def get_parent_list(self):
               """
               Return all the ancestors of this model as a list ordered by MRO.
               Backward compatibility method.
               """
               return list(self.all_parents)
       
    1:     def get_ancestor_link(self, ancestor):
               """
               Return the field on the current model which points to the given
               "ancestor". This is possible an indirect link (a pointer to a parent
               model, which points, eventually, to the ancestor). Used when
               constructing table joins for model inheritance.
       
               Return None if the model isn't an ancestor of this one.
               """
   68:         if ancestor in self.parents:
                   return self.parents[ancestor]
   68:         for parent in self.parents:
                   # Tries to get a link field from the immediate parent
                   parent_link = parent._meta.get_ancestor_link(ancestor)
                   if parent_link:
                       # In case of a proxied model, the first link
                       # of the chain to the ancestor is that parent
                       # links
                       return self.parents[parent] or parent_link
       
    1:     def get_path_to_parent(self, parent):
               """
               Return a list of PathInfos containing the path from the current
               model to the parent model, or an empty list if parent is not a
               parent of the current model.
               """
               if self.model is parent:
                   return []
               # Skip the chain of proxy to the concrete proxied model.
               proxied_model = self.concrete_model
               path = []
               opts = self
               for int_model in self.get_base_chain(parent):
                   if int_model is proxied_model:
                       opts = int_model._meta
                   else:
                       final_field = opts.parents[int_model]
                       targets = (final_field.remote_field.get_related_field(),)
                       opts = int_model._meta
                       path.append(
                           PathInfo(
                               from_opts=final_field.model._meta,
                               to_opts=opts,
                               target_fields=targets,
                               join_field=final_field,
                               m2m=False,
                               direct=True,
                               filtered_relation=None,
                           )
                       )
               return path
       
    1:     def get_path_from_parent(self, parent):
               """
               Return a list of PathInfos containing the path from the parent
               model to the current model, or an empty list if parent is not a
               parent of the current model.
               """
               if self.model is parent:
                   return []
               model = self.concrete_model
               # Get a reversed base chain including both the current and parent
               # models.
               chain = model._meta.get_base_chain(parent)
               chain.reverse()
               chain.append(model)
               # Construct a list of the PathInfos between models in chain.
               path = []
               for i, ancestor in enumerate(chain[:-1]):
                   child = chain[i + 1]
                   link = child._meta.get_ancestor_link(ancestor)
                   path.extend(link.reverse_path_infos)
               return path
       
    1:     def _populate_directed_relation_graph(self):
               """
               This method is used by each model to find its reverse objects. As this
               method is very expensive and is accessed frequently (it looks up every
               field in a model, in every app), it is computed on first access and then
               is set as a property on every model.
               """
   39:         related_objects_graph = defaultdict(list)
       
   39:         all_models = self.apps.get_models(include_auto_created=True)
  469:         for model in all_models:
  430:             opts = model._meta
                   # Abstract model's fields are copied to child models, hence we will
                   # see the fields from the child models.
  430:             if opts.abstract:
                       continue
 5954:             fields_with_relations = (
  552:                 f
 2762:                 for f in opts._get_fields(reverse=False, include_parents=False)
 5216:                 if f.is_relation and f.related_model is not None
                   )
  982:             for f in fields_with_relations:
  552:                 if not isinstance(f.remote_field.model, str):
  552:                     remote_label = f.remote_field.model._meta.concrete_model._meta.label
  552:                     related_objects_graph[remote_label].append(f)
       
  469:         for model in all_models:
                   # Set the relation_tree using the internal __dict__. In this way
                   # we avoid calling the cached property. In attribute lookup,
                   # __dict__ takes precedence over a data descriptor (such as
                   # @cached_property). This means that the _meta._relation_tree is
                   # only called if related_objects is not in __dict__.
  860:             related_objects = related_objects_graph[
  430:                 model._meta.concrete_model._meta.label
                   ]
  430:             model._meta.__dict__["_relation_tree"] = related_objects
               # It seems it is possible that self is not in all_models, so guard
               # against that with default for get().
   39:         return self.__dict__.get("_relation_tree", EMPTY_RELATION_TREE)
       
    2:     @cached_property
    2:     def _relation_tree(self):
   39:         return self._populate_directed_relation_graph()
       
    1:     def _expire_cache(self, forward=True, reverse=True):
               # This method is usually called by apps.cache_clear(), when the
               # registry is finalized, or when a new field is added.
 3149:         if forward:
36492:             for cache_key in self.FORWARD_PROPERTIES:
33451:                 if cache_key in self.__dict__:
  695:                     delattr(self, cache_key)
 3149:         if reverse and not self.abstract:
 6364:             for cache_key in self.REVERSE_PROPERTIES:
 4773:                 if cache_key in self.__dict__:
  145:                     delattr(self, cache_key)
 3149:         self._get_fields_cache = {}
       
    1:     def get_fields(self, include_parents=True, include_hidden=False):
               """
               Return a list of fields associated to the model. By default, include
               forward and reverse fields, fields derived from inheritance, but not
               hidden fields. The returned fields can be changed using the parameters:
       
               - include_parents: include fields derived from inheritance
               - include_hidden:  include fields that have a related_name that
                                  starts with a "+"
               """
  278:         if include_parents is False:
                   include_parents = PROXY_PARENTS
  556:         return self._get_fields(
  278:             include_parents=include_parents, include_hidden=include_hidden
               )
       
    1:     def _get_fields(
               self,
    1:         forward=True,
    1:         reverse=True,
    1:         include_parents=True,
    1:         include_hidden=False,
    1:         topmost_call=True,
           ):
               """
               Internal helper function to return fields of the model.
               * If forward=True, then fields defined on this model are returned.
               * If reverse=True, then relations pointing to this model are returned.
               * If include_hidden=True, then fields with is_hidden=True are returned.
               * The include_parents argument toggles if fields from parent models
                 should be included. It has three values: True, False, and
                 PROXY_PARENTS. When set to PROXY_PARENTS, the call will return all
                 fields defined for the current model or any of its parents in the
                 parent chain to the model's concrete model.
               """
 1259:         if include_parents not in (True, False, PROXY_PARENTS):
                   raise TypeError(
                       "Invalid argument for include_parents: %s" % (include_parents,)
                   )
               # This helper function is used to allow recursion in ``get_fields()``
               # implementation and to provide a fast way for Django's internals to
               # access specific subsets of fields.
       
               # Creates a cache key composed of all arguments
 1259:         cache_key = (forward, reverse, include_parents, include_hidden, topmost_call)
       
 1259:         try:
                   # In order to avoid list manipulation. Always return a shallow copy
                   # of the results.
 1259:             return self._get_fields_cache[cache_key]
 1091:         except KeyError:
 1091:             pass
       
 1091:         fields = []
               # Recursively call _get_fields() on each parent, with the same
               # options provided in this call.
 1091:         if include_parents is not False:
                   # In diamond inheritance it is possible that we see the same model
                   # from two different routes. In that case, avoid adding fields from
                   # the same parent again.
  661:             parent_fields = set()
  661:             for parent in self.parents:
                       if (
                           parent._meta.concrete_model != self.concrete_model
                           and include_parents == PROXY_PARENTS
                       ):
                           continue
                       for obj in parent._meta._get_fields(
                           forward=forward,
                           reverse=reverse,
                           include_parents=include_parents,
                           include_hidden=include_hidden,
                           topmost_call=False,
                       ):
                           if (
                               not getattr(obj, "parent_link", False)
                               or obj.model == self.concrete_model
                           ) and obj not in parent_fields:
                               fields.append(obj)
                               parent_fields.add(obj)
       
 1091:         if reverse and not self.proxy:
                   # Tree is computed once and cached until the app cache is expired.
                   # It is composed of a list of fields pointing to the current model
                   # from other models.
  208:             all_fields = self._relation_tree
  521:             for field in all_fields:
                       # If hidden fields should be included or the relation is not
                       # intentionally hidden, add to the fields dict.
  313:                 if include_hidden or not field.remote_field.hidden:
  313:                     fields.append(field.remote_field)
       
 1091:         if forward:
 1068:             fields += self.local_fields
 1068:             fields += self.local_many_to_many
                   # Private fields are recopied to each child model, and they get a
                   # different model as field.model in each child. Hence we have to
                   # add the private fields separately from the topmost call. If we
                   # did this recursively similar to local_fields, we would get field
                   # instances with field.model != self.model.
 1068:             if topmost_call:
 1068:                 fields += self.private_fields
       
               # In order to avoid list manipulation. Always
               # return a shallow copy of the results
 1091:         fields = make_immutable_fields_list("get_fields()", fields)
       
               # Store result into cache for later access
 1091:         self._get_fields_cache[cache_key] = fields
 1091:         return fields
       
    2:     @cached_property
    2:     def total_unique_constraints(self):
               """
               Return a list of total unique constraints. Useful for determining set
               of fields guaranteed to be unique for all rows.
               """
               return [
                   constraint
                   for constraint in self.constraints
                   if (
                       isinstance(constraint, UniqueConstraint)
                       and constraint.condition is None
                       and not constraint.contains_expressions
                   )
               ]
       
    2:     @cached_property
    2:     def pk_fields(self):
   16:         return composite.unnest([self.pk])
       
    2:     @property
    2:     def is_composite_pk(self):
   16:         return isinstance(self.pk, CompositePrimaryKey)
       
    2:     @cached_property
    2:     def _property_names(self):
               """Return a set of the names of the properties defined on the model."""
   20:         names = set()
   20:         seen = set()
  104:         for klass in self.model.__mro__:
 5022:             names |= {
   24:                 name
 2469:                 for name, value in klass.__dict__.items()
 4878:                 if isinstance(value, property) and name not in seen
                   }
   84:             seen |= set(klass.__dict__)
   20:         return frozenset(names)
       
    2:     @cached_property
    2:     def _non_pk_concrete_field_names(self):
               """
               Return a set of the non-pk concrete field names defined on the model.
               """
    5:         names = []
    5:         all_pk_fields = set(self.pk_fields)
    5:         for parent in self.all_parents:
                   all_pk_fields.update(parent._meta.pk_fields)
   45:         for field in self.concrete_fields:
   40:             if field not in all_pk_fields:
   35:                 names.append(field.name)
   35:                 if field.name != field.attname:
    7:                     names.append(field.attname)
    5:         return frozenset(names)
       
    2:     @cached_property
    2:     def _reverse_one_to_one_field_names(self):
               """
               Return a set of reverse one to one field names pointing to the current
               model.
               """
   73:         return frozenset(
   30:             field.name for field in self.related_objects if field.one_to_one
               )
       
    2:     @cached_property
    2:     def db_returning_fields(self):
               """
               Private API intended only to be used by Django itself.
               Fields to be returned after a database insert.
               """
  293:         return [
   17:             field
  147:             for field in self._get_fields(
   18:                 forward=True, reverse=False, include_parents=PROXY_PARENTS
                   )
  111:             if getattr(field, "db_returning", False)
               ]
