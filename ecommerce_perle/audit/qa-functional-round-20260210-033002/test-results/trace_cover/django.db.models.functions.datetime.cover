    1: from datetime import datetime
       
    1: from django.conf import settings
    1: from django.db.models.expressions import Func
    1: from django.db.models.fields import (
           DateField,
           DateTimeField,
           DurationField,
           Field,
           IntegerField,
           TimeField,
       )
    1: from django.db.models.lookups import (
           Transform,
           YearExact,
           YearGt,
           YearGte,
           YearLt,
           YearLte,
       )
    1: from django.utils import timezone
       
       
    2: class TimezoneMixin:
    1:     tzinfo = None
       
    1:     def get_tzname(self):
               # Timezone conversions must happen to the input datetime *before*
               # applying a function. 2015-12-31 23:00:00 -02:00 is stored in the
               # database as 2016-01-01 01:00:00 +00:00. Any results should be
               # based on the input datetime not the stored datetime.
               tzname = None
               if settings.USE_TZ:
                   if self.tzinfo is None:
                       tzname = timezone.get_current_timezone_name()
                   else:
                       tzname = timezone._get_timezone_name(self.tzinfo)
               return tzname
       
       
    2: class Extract(TimezoneMixin, Transform):
    1:     lookup_name = None
    1:     output_field = IntegerField()
       
    1:     def __init__(self, expression, lookup_name=None, tzinfo=None, **extra):
               if self.lookup_name is None:
                   self.lookup_name = lookup_name
               if self.lookup_name is None:
                   raise ValueError("lookup_name must be provided")
               self.tzinfo = tzinfo
               super().__init__(expression, **extra)
       
    1:     def as_sql(self, compiler, connection):
               sql, params = compiler.compile(self.lhs)
               lhs_output_field = self.lhs.output_field
               if isinstance(lhs_output_field, DateTimeField):
                   tzname = self.get_tzname()
                   sql, params = connection.ops.datetime_extract_sql(
                       self.lookup_name, sql, tuple(params), tzname
                   )
               elif self.tzinfo is not None:
                   raise ValueError("tzinfo can only be used with DateTimeField.")
               elif isinstance(lhs_output_field, DateField):
                   sql, params = connection.ops.date_extract_sql(
                       self.lookup_name, sql, tuple(params)
                   )
               elif isinstance(lhs_output_field, TimeField):
                   sql, params = connection.ops.time_extract_sql(
                       self.lookup_name, sql, tuple(params)
                   )
               elif isinstance(lhs_output_field, DurationField):
                   if not connection.features.has_native_duration_field:
                       raise ValueError(
                           "Extract requires native DurationField database support."
                       )
                   sql, params = connection.ops.time_extract_sql(
                       self.lookup_name, sql, tuple(params)
                   )
               else:
                   # resolve_expression has already validated the output_field so this
                   # assert should never be hit.
                   assert False, "Tried to Extract from an invalid type."
               return sql, params
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               copy = super().resolve_expression(
                   query, allow_joins, reuse, summarize, for_save
               )
               field = getattr(copy.lhs, "output_field", None)
               if field is None:
                   return copy
               if not isinstance(field, (DateField, DateTimeField, TimeField, DurationField)):
                   raise ValueError(
                       "Extract input expression must be DateField, DateTimeField, "
                       "TimeField, or DurationField."
                   )
               # Passing dates to functions expecting datetimes is most likely a mistake.
               if type(field) is DateField and copy.lookup_name in (
                   "hour",
                   "minute",
                   "second",
               ):
                   raise ValueError(
                       "Cannot extract time component '%s' from DateField '%s'."
                       % (copy.lookup_name, field.name)
                   )
               if isinstance(field, DurationField) and copy.lookup_name in (
                   "year",
                   "iso_year",
                   "month",
                   "week",
                   "week_day",
                   "iso_week_day",
                   "quarter",
               ):
                   raise ValueError(
                       "Cannot extract component '%s' from DurationField '%s'."
                       % (copy.lookup_name, field.name)
                   )
               return copy
       
       
    2: class ExtractYear(Extract):
    1:     lookup_name = "year"
       
       
    2: class ExtractIsoYear(Extract):
    1:     """Return the ISO-8601 week-numbering year."""
       
    1:     lookup_name = "iso_year"
       
       
    2: class ExtractMonth(Extract):
    1:     lookup_name = "month"
       
       
    2: class ExtractDay(Extract):
    1:     lookup_name = "day"
       
       
    2: class ExtractWeek(Extract):
    1:     """
           Return 1-52 or 53, based on ISO-8601, i.e., Monday is the first of the
           week.
           """
       
    1:     lookup_name = "week"
       
       
    2: class ExtractWeekDay(Extract):
    1:     """
           Return Sunday=1 through Saturday=7.
       
           To replicate this in Python: (mydatetime.isoweekday() % 7) + 1
           """
       
    1:     lookup_name = "week_day"
       
       
    2: class ExtractIsoWeekDay(Extract):
    1:     """Return Monday=1 through Sunday=7, based on ISO-8601."""
       
    1:     lookup_name = "iso_week_day"
       
       
    2: class ExtractQuarter(Extract):
    1:     lookup_name = "quarter"
       
       
    2: class ExtractHour(Extract):
    1:     lookup_name = "hour"
       
       
    2: class ExtractMinute(Extract):
    1:     lookup_name = "minute"
       
       
    2: class ExtractSecond(Extract):
    1:     lookup_name = "second"
       
       
    1: DateField.register_lookup(ExtractYear)
    1: DateField.register_lookup(ExtractMonth)
    1: DateField.register_lookup(ExtractDay)
    1: DateField.register_lookup(ExtractWeekDay)
    1: DateField.register_lookup(ExtractIsoWeekDay)
    1: DateField.register_lookup(ExtractWeek)
    1: DateField.register_lookup(ExtractIsoYear)
    1: DateField.register_lookup(ExtractQuarter)
       
    1: TimeField.register_lookup(ExtractHour)
    1: TimeField.register_lookup(ExtractMinute)
    1: TimeField.register_lookup(ExtractSecond)
       
    1: DateTimeField.register_lookup(ExtractHour)
    1: DateTimeField.register_lookup(ExtractMinute)
    1: DateTimeField.register_lookup(ExtractSecond)
       
    1: ExtractYear.register_lookup(YearExact)
    1: ExtractYear.register_lookup(YearGt)
    1: ExtractYear.register_lookup(YearGte)
    1: ExtractYear.register_lookup(YearLt)
    1: ExtractYear.register_lookup(YearLte)
       
    1: ExtractIsoYear.register_lookup(YearExact)
    1: ExtractIsoYear.register_lookup(YearGt)
    1: ExtractIsoYear.register_lookup(YearGte)
    1: ExtractIsoYear.register_lookup(YearLt)
    1: ExtractIsoYear.register_lookup(YearLte)
       
       
    2: class Now(Func):
    1:     template = "CURRENT_TIMESTAMP"
    1:     output_field = DateTimeField()
       
    1:     def as_postgresql(self, compiler, connection, **extra_context):
               # PostgreSQL's CURRENT_TIMESTAMP means "the time at the start of the
               # transaction". Use STATEMENT_TIMESTAMP to be cross-compatible with
               # other databases.
               return self.as_sql(
                   compiler, connection, template="STATEMENT_TIMESTAMP()", **extra_context
               )
       
    1:     def as_mysql(self, compiler, connection, **extra_context):
               return self.as_sql(
                   compiler, connection, template="CURRENT_TIMESTAMP(6)", **extra_context
               )
       
    1:     def as_sqlite(self, compiler, connection, **extra_context):
               return self.as_sql(
                   compiler,
                   connection,
                   template="STRFTIME('%%%%Y-%%%%m-%%%%d %%%%H:%%%%M:%%%%f', 'NOW')",
                   **extra_context,
               )
       
    1:     def as_oracle(self, compiler, connection, **extra_context):
               return self.as_sql(
                   compiler, connection, template="LOCALTIMESTAMP", **extra_context
               )
       
       
    2: class TruncBase(TimezoneMixin, Transform):
    1:     kind = None
    1:     tzinfo = None
       
    1:     def __init__(
               self,
               expression,
    1:         output_field=None,
    1:         tzinfo=None,
               **extra,
           ):
               self.tzinfo = tzinfo
               super().__init__(expression, output_field=output_field, **extra)
       
    1:     def as_sql(self, compiler, connection):
               sql, params = compiler.compile(self.lhs)
               tzname = None
               if isinstance(self.lhs.output_field, DateTimeField):
                   tzname = self.get_tzname()
               elif self.tzinfo is not None:
                   raise ValueError("tzinfo can only be used with DateTimeField.")
               if isinstance(self.output_field, DateTimeField):
                   sql, params = connection.ops.datetime_trunc_sql(
                       self.kind, sql, tuple(params), tzname
                   )
               elif isinstance(self.output_field, DateField):
                   sql, params = connection.ops.date_trunc_sql(
                       self.kind, sql, tuple(params), tzname
                   )
               elif isinstance(self.output_field, TimeField):
                   sql, params = connection.ops.time_trunc_sql(
                       self.kind, sql, tuple(params), tzname
                   )
               else:
                   raise ValueError(
                       "Trunc only valid on DateField, TimeField, or DateTimeField."
                   )
               return sql, params
       
    1:     def resolve_expression(
    1:         self, query=None, allow_joins=True, reuse=None, summarize=False, for_save=False
           ):
               copy = super().resolve_expression(
                   query, allow_joins, reuse, summarize, for_save
               )
               field = copy.lhs.output_field
               # DateTimeField is a subclass of DateField so this works for both.
               if not isinstance(field, (DateField, TimeField)):
                   raise TypeError(
                       "%r isn't a DateField, TimeField, or DateTimeField." % field.name
                   )
               # If self.output_field was None, then accessing the field will trigger
               # the resolver to assign it to self.lhs.output_field.
               if not isinstance(copy.output_field, (DateField, DateTimeField, TimeField)):
                   raise ValueError(
                       "output_field must be either DateField, TimeField, or DateTimeField"
                   )
               # Passing dates or times to functions expecting datetimes is most
               # likely a mistake.
               class_output_field = (
                   self.__class__.output_field
                   if isinstance(self.__class__.output_field, Field)
                   else None
               )
               output_field = class_output_field or copy.output_field
               has_explicit_output_field = (
                   class_output_field or field.__class__ is not copy.output_field.__class__
               )
               if type(field) is DateField and (
                   isinstance(output_field, DateTimeField)
                   or copy.kind in ("hour", "minute", "second", "time")
               ):
                   raise ValueError(
                       "Cannot truncate DateField '%s' to %s."
                       % (
                           field.name,
                           (
                               output_field.__class__.__name__
                               if has_explicit_output_field
                               else "DateTimeField"
                           ),
                       )
                   )
               elif isinstance(field, TimeField) and (
                   isinstance(output_field, DateTimeField)
                   or copy.kind in ("year", "quarter", "month", "week", "day", "date")
               ):
                   raise ValueError(
                       "Cannot truncate TimeField '%s' to %s."
                       % (
                           field.name,
                           (
                               output_field.__class__.__name__
                               if has_explicit_output_field
                               else "DateTimeField"
                           ),
                       )
                   )
               return copy
       
    1:     def convert_value(self, value, expression, connection):
               if isinstance(self.output_field, DateTimeField):
                   if not settings.USE_TZ:
                       pass
                   elif value is not None:
                       value = value.replace(tzinfo=None)
                       value = timezone.make_aware(value, self.tzinfo)
                   elif not connection.features.has_zoneinfo_database:
                       raise ValueError(
                           "Database returned an invalid datetime value. Are time "
                           "zone definitions for your database installed?"
                       )
               elif isinstance(value, datetime):
                   if value is None:
                       pass
                   elif isinstance(self.output_field, DateField):
                       value = value.date()
                   elif isinstance(self.output_field, TimeField):
                       value = value.time()
               return value
       
       
    2: class Trunc(TruncBase):
    1:     def __init__(
               self,
               expression,
               kind,
    1:         output_field=None,
    1:         tzinfo=None,
               **extra,
           ):
               self.kind = kind
               super().__init__(expression, output_field=output_field, tzinfo=tzinfo, **extra)
       
       
    2: class TruncYear(TruncBase):
    1:     kind = "year"
       
       
    2: class TruncQuarter(TruncBase):
    1:     kind = "quarter"
       
       
    2: class TruncMonth(TruncBase):
    1:     kind = "month"
       
       
    2: class TruncWeek(TruncBase):
    1:     """Truncate to midnight on the Monday of the week."""
       
    1:     kind = "week"
       
       
    2: class TruncDay(TruncBase):
    1:     kind = "day"
       
       
    2: class TruncDate(TruncBase):
    1:     kind = "date"
    1:     lookup_name = "date"
    1:     output_field = DateField()
       
    1:     def as_sql(self, compiler, connection):
               # Cast to date rather than truncate to date.
               sql, params = compiler.compile(self.lhs)
               tzname = self.get_tzname()
               return connection.ops.datetime_cast_date_sql(sql, tuple(params), tzname)
       
       
    2: class TruncTime(TruncBase):
    1:     kind = "time"
    1:     lookup_name = "time"
    1:     output_field = TimeField()
       
    1:     def as_sql(self, compiler, connection):
               # Cast to time rather than truncate to time.
               sql, params = compiler.compile(self.lhs)
               tzname = self.get_tzname()
               return connection.ops.datetime_cast_time_sql(sql, tuple(params), tzname)
       
       
    2: class TruncHour(TruncBase):
    1:     kind = "hour"
       
       
    2: class TruncMinute(TruncBase):
    1:     kind = "minute"
       
       
    2: class TruncSecond(TruncBase):
    1:     kind = "second"
       
       
    1: DateTimeField.register_lookup(TruncDate)
    1: DateTimeField.register_lookup(TruncTime)
