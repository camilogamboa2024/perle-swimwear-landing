    1: from pathlib import Path
       
    1: from django.conf import settings
       
    1: from . import Error, Tags, register
       
       
    2: @register(Tags.files)
    2: def check_setting_file_upload_temp_dir(app_configs, **kwargs):
    1:     setting = getattr(settings, "FILE_UPLOAD_TEMP_DIR", None)
    1:     if setting and not Path(setting).is_dir():
               return [
                   Error(
                       f"The FILE_UPLOAD_TEMP_DIR setting refers to the nonexistent "
                       f"directory '{setting}'.",
                       id="files.E001",
                   ),
               ]
    1:     return []
