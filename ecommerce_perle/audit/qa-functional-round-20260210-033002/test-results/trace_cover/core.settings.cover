    1: import os
    1: import sys
       
    1: import dj_database_url
    1: from pathlib import Path
       
    1: from django.core.exceptions import ImproperlyConfigured
       
    1: BASE_DIR = Path(__file__).resolve().parent.parent
    1: RUNNING_TESTS = 'test' in sys.argv
    1: SECRET_KEY = os.getenv('DJANGO_SECRET_KEY', 'dev-key-change-me')
    1: DEBUG = os.getenv('DEBUG', '1') == '1'
    1: if not DEBUG and SECRET_KEY == 'dev-key-change-me':
           raise ImproperlyConfigured('Configura DJANGO_SECRET_KEY para producción.')
    4: ALLOWED_HOSTS = [h.strip() for h in os.getenv('ALLOWED_HOSTS', 'localhost,127.0.0.1').split(',') if h.strip()]
    3: CSRF_TRUSTED_ORIGINS = [x.strip() for x in os.getenv('CSRF_TRUSTED_ORIGINS', '').split(',') if x.strip()]
       
    1: INSTALLED_APPS = [
           'django.contrib.admin',
           'django.contrib.auth',
           'django.contrib.contenttypes',
           'django.contrib.sessions',
           'django.contrib.messages',
           'django.contrib.staticfiles',
           'rest_framework',
           'apps.catalog',
           'apps.inventory.apps.InventoryConfig',
           'apps.customers',
           'apps.orders',
           'apps.checkout',
           'apps.dashboard',
       ]
       
    1: MIDDLEWARE = [
           'django.middleware.security.SecurityMiddleware',
           'whitenoise.middleware.WhiteNoiseMiddleware',
           'django.contrib.sessions.middleware.SessionMiddleware',
           'django.middleware.common.CommonMiddleware',
           'django.middleware.csrf.CsrfViewMiddleware',
           'django.contrib.auth.middleware.AuthenticationMiddleware',
           'django.contrib.messages.middleware.MessageMiddleware',
           'django.middleware.clickjacking.XFrameOptionsMiddleware',
       ]
       
    1: ROOT_URLCONF = 'core.urls'
       
    1: TEMPLATES = [
    1:     {
    1:         'BACKEND': 'django.template.backends.django.DjangoTemplates',
    1:         'DIRS': [BASE_DIR / 'templates'],
    1:         'APP_DIRS': True,
    1:         'OPTIONS': {
    1:             'context_processors': [
                       'django.template.context_processors.request',
                       'django.contrib.auth.context_processors.auth',
                       'django.contrib.messages.context_processors.messages',
                       'core.context_processors.brand_globals',
                   ],
               },
           },
       ]
       
    1: WSGI_APPLICATION = 'core.wsgi.application'
    1: ASGI_APPLICATION = 'core.asgi.application'
       
    1: DATABASE_URL = os.getenv('DATABASE_URL', '').strip()
    1: if DATABASE_URL:
           DATABASES = {
               'default': dj_database_url.parse(
                   DATABASE_URL,
                   conn_max_age=int(os.getenv('DB_CONN_MAX_AGE', '600')),
                   ssl_require=os.getenv('DB_SSL_REQUIRE', '1') == '1',
               )
           }
       else:
    1:     if not DEBUG and os.getenv('DB_ENGINE', 'django.db.backends.sqlite3') == 'django.db.backends.sqlite3':
               raise ImproperlyConfigured('En producción debes configurar DATABASE_URL o DB_ENGINE no-SQLite.')
    1:     DATABASES = {
    2:         'default': {
    1:             'ENGINE': os.getenv('DB_ENGINE', 'django.db.backends.sqlite3'),
    1:             'NAME': os.getenv('DB_NAME', BASE_DIR / 'db.sqlite3'),
    1:             'USER': os.getenv('DB_USER', ''),
    1:             'PASSWORD': os.getenv('DB_PASSWORD', ''),
    1:             'HOST': os.getenv('DB_HOST', ''),
    1:             'PORT': os.getenv('DB_PORT', ''),
    1:             'CONN_MAX_AGE': int(os.getenv('DB_CONN_MAX_AGE', '600')),
               }
           }
       
       
    1: AUTH_PASSWORD_VALIDATORS = [
    1:     {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator'},
    1:     {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator', 'OPTIONS': {'min_length': 9}},
    1:     {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
    1:     {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator'},
       ]
    1: LANGUAGE_CODE = 'es-co'
    1: TIME_ZONE = 'America/Bogota'
    1: USE_I18N = True
    1: USE_TZ = True
    1: STATIC_URL = '/static/'
    1: STATICFILES_DIRS = [BASE_DIR / 'static']
    1: STATIC_ROOT = BASE_DIR / 'staticfiles'
    1: DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
    1: EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
    1: SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
       
    1: WHATSAPP_PHONE = os.getenv('WHATSAPP_PHONE', '')
    1: CURRENCY_USD_RATE = float(os.getenv('CURRENCY_USD_RATE', '0.00026'))
       
    1: REST_FRAMEWORK = {
    2:     'DEFAULT_THROTTLE_RATES': {
    1:         'checkout': '20/hour',
    1:         'sensitive': '60/hour',
           }
       }
       
    1: STATICFILES_STORAGE_BACKEND = (
    1:     'django.contrib.staticfiles.storage.StaticFilesStorage'
    1:     if RUNNING_TESTS
           else 'whitenoise.storage.CompressedManifestStaticFilesStorage'
       )
    1: STORAGES = {
    1:     'default': {'BACKEND': 'django.core.files.storage.FileSystemStorage'},
    1:     'staticfiles': {'BACKEND': STATICFILES_STORAGE_BACKEND},
       }
       
    1: SECURE_CONTENT_TYPE_NOSNIFF = True
    1: X_FRAME_OPTIONS = 'DENY'
       
    1: if not DEBUG:
           CSRF_COOKIE_SECURE = True
           SESSION_COOKIE_SECURE = True
           SECURE_SSL_REDIRECT = os.getenv('SECURE_SSL_REDIRECT', '1') == '1'
           SECURE_HSTS_SECONDS = int(os.getenv('SECURE_HSTS_SECONDS', '300'))
           SECURE_HSTS_INCLUDE_SUBDOMAINS = os.getenv('SECURE_HSTS_INCLUDE_SUBDOMAINS', '1') == '1'
           SECURE_HSTS_PRELOAD = os.getenv('SECURE_HSTS_PRELOAD', '0') == '1'
       
       
    1: SECURE_REFERRER_POLICY = os.getenv('SECURE_REFERRER_POLICY', 'strict-origin-when-cross-origin')
    1: SECURE_CROSS_ORIGIN_OPENER_POLICY = os.getenv('SECURE_CROSS_ORIGIN_OPENER_POLICY', 'same-origin')
    1: SESSION_COOKIE_HTTPONLY = True
    1: CSRF_COOKIE_HTTPONLY = os.getenv('CSRF_COOKIE_HTTPONLY', '0') == '1'
    1: SESSION_COOKIE_SAMESITE = os.getenv('SESSION_COOKIE_SAMESITE', 'Lax')
    1: CSRF_COOKIE_SAMESITE = os.getenv('CSRF_COOKIE_SAMESITE', 'Lax')
