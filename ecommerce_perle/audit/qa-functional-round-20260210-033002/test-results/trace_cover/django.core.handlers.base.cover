    1: import asyncio
    1: import logging
    1: import types
       
    1: from asgiref.sync import async_to_sync, iscoroutinefunction, sync_to_async
       
    1: from django.conf import settings
    1: from django.core.exceptions import ImproperlyConfigured, MiddlewareNotUsed
    1: from django.core.signals import request_finished
    1: from django.db import connections, transaction
    1: from django.urls import get_resolver, set_urlconf
    1: from django.utils.log import log_response
    1: from django.utils.module_loading import import_string
       
    1: from .exception import convert_exception_to_response
       
    1: logger = logging.getLogger("django.request")
       
       
    2: class BaseHandler:
    1:     _view_middleware = None
    1:     _template_response_middleware = None
    1:     _exception_middleware = None
    1:     _middleware_chain = None
       
    1:     def load_middleware(self, is_async=False):
               """
               Populate middleware lists from settings.MIDDLEWARE.
       
               Must be called after the environment is fixed (see __call__ in subclasses).
               """
   18:         self._view_middleware = []
   18:         self._template_response_middleware = []
   18:         self._exception_middleware = []
       
   18:         get_response = self._get_response_async if is_async else self._get_response
   18:         handler = convert_exception_to_response(get_response)
   18:         handler_is_async = is_async
  162:         for middleware_path in reversed(settings.MIDDLEWARE):
  144:             middleware = import_string(middleware_path)
  144:             middleware_can_sync = getattr(middleware, "sync_capable", True)
  144:             middleware_can_async = getattr(middleware, "async_capable", False)
  144:             if not middleware_can_sync and not middleware_can_async:
                       raise RuntimeError(
                           "Middleware %s must have at least one of "
                           "sync_capable/async_capable set to True." % middleware_path
                       )
  144:             elif not handler_is_async and middleware_can_sync:
  144:                 middleware_is_async = False
                   else:
                       middleware_is_async = middleware_can_async
  144:             try:
                       # Adapt handler, if needed.
  288:                 adapted_handler = self.adapt_method_mode(
  144:                     middleware_is_async,
  144:                     handler,
  144:                     handler_is_async,
  144:                     debug=settings.DEBUG,
  144:                     name="middleware %s" % middleware_path,
                       )
  144:                 mw_instance = middleware(adapted_handler)
                   except MiddlewareNotUsed as exc:
                       if settings.DEBUG:
                           if str(exc):
                               logger.debug("MiddlewareNotUsed(%r): %s", middleware_path, exc)
                           else:
                               logger.debug("MiddlewareNotUsed: %r", middleware_path)
                       continue
                   else:
  144:                 handler = adapted_handler
       
  144:             if mw_instance is None:
                       raise ImproperlyConfigured(
                           "Middleware factory %s returned None." % middleware_path
                       )
       
  144:             if hasattr(mw_instance, "process_view"):
   36:                 self._view_middleware.insert(
   18:                     0,
   18:                     self.adapt_method_mode(is_async, mw_instance.process_view),
                       )
  144:             if hasattr(mw_instance, "process_template_response"):
                       self._template_response_middleware.append(
                           self.adapt_method_mode(
                               is_async, mw_instance.process_template_response
                           ),
                       )
  144:             if hasattr(mw_instance, "process_exception"):
                       # The exception-handling stack is still always synchronous for
                       # now, so adapt that way.
                       self._exception_middleware.append(
                           self.adapt_method_mode(False, mw_instance.process_exception),
                       )
       
  144:             handler = convert_exception_to_response(mw_instance)
  144:             handler_is_async = middleware_is_async
       
               # Adapt the top of the stack, if needed.
   18:         handler = self.adapt_method_mode(is_async, handler, handler_is_async)
               # We only assign to this when initialization is complete as it is used
               # as a flag for initialization being complete.
   18:         self._middleware_chain = handler
       
    1:     def adapt_method_mode(
               self,
               is_async,
               method,
    1:         method_is_async=None,
    1:         debug=False,
    1:         name=None,
           ):
               """
               Adapt a method to be in the correct "mode":
               - If is_async is False:
                 - Synchronous methods are left alone
                 - Asynchronous methods are wrapped with async_to_sync
               - If is_async is True:
                 - Synchronous methods are wrapped with sync_to_async()
                 - Asynchronous methods are left alone
               """
  180:         if method_is_async is None:
   18:             method_is_async = iscoroutinefunction(method)
  180:         if debug and not name:
                   name = name or "method %s()" % method.__qualname__
  180:         if is_async:
                   if not method_is_async:
                       if debug:
                           logger.debug("Synchronous handler adapted for %s.", name)
                       return sync_to_async(method, thread_sensitive=True)
  180:         elif method_is_async:
                   if debug:
                       logger.debug("Asynchronous handler adapted for %s.", name)
                   return async_to_sync(method)
  180:         return method
       
    1:     def get_response(self, request):
               """Return an HttpResponse object for the given HttpRequest."""
               # Setup default url resolver for this thread
   43:         set_urlconf(settings.ROOT_URLCONF)
   43:         response = self._middleware_chain(request)
   43:         response._resource_closers.append(request.close)
   43:         if response.status_code >= 400:
   20:             log_response(
   10:                 "%s: %s",
   10:                 response.reason_phrase,
   10:                 request.path,
   10:                 response=response,
   10:                 request=request,
                   )
   43:         return response
       
    1:     async def get_response_async(self, request):
               """
               Asynchronous version of get_response.
       
               Funneling everything, including WSGI, into a single async
               get_response() is too slow. Avoid the context switch by using
               a separate async response path.
               """
               # Setup default url resolver for this thread.
               set_urlconf(settings.ROOT_URLCONF)
               response = await self._middleware_chain(request)
               response._resource_closers.append(request.close)
               if response.status_code >= 400:
                   await sync_to_async(log_response, thread_sensitive=False)(
                       "%s: %s",
                       response.reason_phrase,
                       request.path,
                       response=response,
                       request=request,
                   )
               return response
       
    1:     def _get_response(self, request):
               """
               Resolve and call the view, then apply view, exception, and
               template_response middleware. This method is everything that happens
               inside the request/response middleware.
               """
   43:         response = None
   43:         callback, callback_args, callback_kwargs = self.resolve_request(request)
       
               # Apply view middleware
   86:         for middleware_method in self._view_middleware:
   86:             response = middleware_method(
   43:                 request, callback, callback_args, callback_kwargs
                   )
   43:             if response:
                       break
       
   43:         if response is None:
   43:             wrapped_callback = self.make_view_atomic(callback)
                   # If it is an asynchronous view, run it in a subthread.
   43:             if iscoroutinefunction(wrapped_callback):
                       wrapped_callback = async_to_sync(wrapped_callback)
   43:             try:
   43:                 response = wrapped_callback(request, *callback_args, **callback_kwargs)
                   except Exception as e:
                       response = self.process_exception_by_middleware(e, request)
                       if response is None:
                           raise
       
               # Complain if the view returned None (a common error).
   43:         self.check_response(response, callback)
       
               # If the response supports deferred rendering, apply template
               # response middleware and then render the response
   43:         if hasattr(response, "render") and callable(response.render):
   23:             for middleware_method in self._template_response_middleware:
                       response = middleware_method(request, response)
                       # Complain if the template response middleware returned None
                       # (a common error).
                       self.check_response(
                           response,
                           middleware_method,
                           name="%s.process_template_response"
                           % (middleware_method.__self__.__class__.__name__,),
                       )
   23:             try:
   23:                 response = response.render()
                   except Exception as e:
                       response = self.process_exception_by_middleware(e, request)
                       if response is None:
                           raise
       
   43:         return response
       
    1:     async def _get_response_async(self, request):
               """
               Resolve and call the view, then apply view, exception, and
               template_response middleware. This method is everything that happens
               inside the request/response middleware.
               """
               response = None
               callback, callback_args, callback_kwargs = self.resolve_request(request)
       
               # Apply view middleware.
               for middleware_method in self._view_middleware:
                   response = await middleware_method(
                       request, callback, callback_args, callback_kwargs
                   )
                   if response:
                       break
       
               if response is None:
                   wrapped_callback = self.make_view_atomic(callback)
                   # If it is a synchronous view, run it in a subthread
                   if not iscoroutinefunction(wrapped_callback):
                       wrapped_callback = sync_to_async(
                           wrapped_callback, thread_sensitive=True
                       )
                   try:
                       response = await wrapped_callback(
                           request, *callback_args, **callback_kwargs
                       )
                   except Exception as e:
                       response = await sync_to_async(
                           self.process_exception_by_middleware,
                           thread_sensitive=True,
                       )(e, request)
                       if response is None:
                           raise
       
               # Complain if the view returned None or an uncalled coroutine.
               self.check_response(response, callback)
       
               # If the response supports deferred rendering, apply template
               # response middleware and then render the response
               if hasattr(response, "render") and callable(response.render):
                   for middleware_method in self._template_response_middleware:
                       response = await middleware_method(request, response)
                       # Complain if the template response middleware returned None or
                       # an uncalled coroutine.
                       self.check_response(
                           response,
                           middleware_method,
                           name="%s.process_template_response"
                           % (middleware_method.__self__.__class__.__name__,),
                       )
                   try:
                       if iscoroutinefunction(response.render):
                           response = await response.render()
                       else:
                           response = await sync_to_async(
                               response.render, thread_sensitive=True
                           )()
                   except Exception as e:
                       response = await sync_to_async(
                           self.process_exception_by_middleware,
                           thread_sensitive=True,
                       )(e, request)
                       if response is None:
                           raise
       
               # Make sure the response is not a coroutine
               if asyncio.iscoroutine(response):
                   raise RuntimeError("Response is still a coroutine.")
               return response
       
    1:     def resolve_request(self, request):
               """
               Retrieve/set the urlconf for the request. Return the view resolved,
               with its args and kwargs.
               """
               # Work out the resolver.
   43:         if hasattr(request, "urlconf"):
                   urlconf = request.urlconf
                   set_urlconf(urlconf)
                   resolver = get_resolver(urlconf)
               else:
   43:             resolver = get_resolver()
               # Resolve the view, and assign the match object back to the request.
   43:         resolver_match = resolver.resolve(request.path_info)
   43:         request.resolver_match = resolver_match
   43:         return resolver_match
       
    1:     def check_response(self, response, callback, name=None):
               """
               Raise an error if the view returned None or an uncalled coroutine.
               """
   43:         if not (response is None or asyncio.iscoroutine(response)):
   43:             return
               if not name:
                   if isinstance(callback, types.FunctionType):  # FBV
                       name = "The view %s.%s" % (callback.__module__, callback.__name__)
                   else:  # CBV
                       name = "The view %s.%s.__call__" % (
                           callback.__module__,
                           callback.__class__.__name__,
                       )
               if response is None:
                   raise ValueError(
                       "%s didn't return an HttpResponse object. It returned None "
                       "instead." % name
                   )
               elif asyncio.iscoroutine(response):
                   raise ValueError(
                       "%s didn't return an HttpResponse object. It returned an "
                       "unawaited coroutine instead. You may need to add an 'await' "
                       "into your view." % name
                   )
       
           # Other utility methods.
       
    1:     def make_view_atomic(self, view):
   43:         non_atomic_requests = getattr(view, "_non_atomic_requests", set())
   86:         for alias, settings_dict in connections.settings.items():
   43:             if settings_dict["ATOMIC_REQUESTS"] and alias not in non_atomic_requests:
                       if iscoroutinefunction(view):
                           raise RuntimeError(
                               "You cannot use ATOMIC_REQUESTS with async views."
                           )
                       view = transaction.atomic(using=alias)(view)
   43:         return view
       
    1:     def process_exception_by_middleware(self, exception, request):
               """
               Pass the exception to the exception middleware. If no middleware
               return a response for this exception, return None.
               """
               for middleware_method in self._exception_middleware:
                   response = middleware_method(request, exception)
                   if response:
                       return response
               return None
       
       
    1: def reset_urlconf(sender, **kwargs):
           """Reset the URLconf after each request is finished."""
   43:     set_urlconf(None)
       
       
    1: request_finished.connect(reset_urlconf)
