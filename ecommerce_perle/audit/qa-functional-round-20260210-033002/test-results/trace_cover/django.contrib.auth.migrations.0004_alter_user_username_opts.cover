    1: from django.contrib.auth import validators
    1: from django.db import migrations, models
       
       
    2: class Migration(migrations.Migration):
    1:     dependencies = [
    1:         ("auth", "0003_alter_user_email_max_length"),
           ]
       
           # No database changes; modifies validators and error_messages (#13147).
    1:     operations = [
    2:         migrations.AlterField(
    1:             model_name="user",
    1:             name="username",
    2:             field=models.CharField(
    1:                 error_messages={"unique": "A user with that username already exists."},
    1:                 max_length=30,
    1:                 validators=[validators.UnicodeUsernameValidator()],
                       help_text=(
    1:                     "Required. 30 characters or fewer. Letters, digits and @/./+/-/_ "
                           "only."
                       ),
    1:                 unique=True,
    1:                 verbose_name="username",
                   ),
               ),
           ]
