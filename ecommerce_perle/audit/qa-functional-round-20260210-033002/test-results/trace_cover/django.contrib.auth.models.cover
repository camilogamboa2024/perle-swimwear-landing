    1: from collections.abc import Iterable
       
    1: from django.apps import apps
    1: from django.contrib import auth
    1: from django.contrib.auth.base_user import AbstractBaseUser, BaseUserManager
    1: from django.contrib.auth.hashers import make_password
    1: from django.contrib.contenttypes.models import ContentType
    1: from django.core.exceptions import PermissionDenied
    1: from django.core.mail import send_mail
    1: from django.db import models
    1: from django.db.models.manager import EmptyManager
    1: from django.utils import timezone
    1: from django.utils.translation import gettext_lazy as _
       
    1: from .validators import UnicodeUsernameValidator
       
       
    1: def update_last_login(sender, user, **kwargs):
           """
           A signal receiver which updates the last_login date for
           the user logging in.
           """
    2:     user.last_login = timezone.now()
    2:     user.save(update_fields=["last_login"])
       
       
    2: class PermissionManager(models.Manager):
    1:     use_in_migrations = True
       
    1:     def get_by_natural_key(self, codename, app_label, model):
               return self.get(
                   codename=codename,
                   content_type=ContentType.objects.db_manager(self.db).get_by_natural_key(
                       app_label, model
                   ),
               )
       
       
    2: class Permission(models.Model):
    1:     """
           The permissions system provides a way to assign permissions to specific
           users and groups of users.
       
           The permission system is used by the Django admin site, but may also be
           useful in your own code. The Django admin site uses permissions as follows:
       
               - The "add" permission limits the user's ability to view the "add" form
                 and add an object.
               - The "change" permission limits a user's ability to view the change
                 list, view the "change" form and change an object.
               - The "delete" permission limits the ability to delete an object.
               - The "view" permission limits the ability to view an object.
       
           Permissions are set globally per type of object, not per specific object
           instance. It is possible to say "Mary may change news stories," but it's
           not currently possible to say "Mary may change news stories, but only the
           ones she created herself" or "Mary may only change news stories that have a
           certain status or publication date."
       
           The permissions listed above are automatically created for each model.
           """
       
    1:     name = models.CharField(_("name"), max_length=255)
    2:     content_type = models.ForeignKey(
    1:         ContentType,
    1:         models.CASCADE,
    1:         verbose_name=_("content type"),
           )
    1:     codename = models.CharField(_("codename"), max_length=100)
       
    1:     objects = PermissionManager()
       
    2:     class Meta:
    1:         verbose_name = _("permission")
    1:         verbose_name_plural = _("permissions")
    1:         unique_together = [["content_type", "codename"]]
    1:         ordering = ["content_type__app_label", "content_type__model", "codename"]
       
    1:     def __str__(self):
               return "%s | %s" % (self.content_type, self.name)
       
    1:     def natural_key(self):
               return (self.codename,) + self.content_type.natural_key()
       
    1:     natural_key.dependencies = ["contenttypes.contenttype"]
       
       
    2: class GroupManager(models.Manager):
    1:     """
           The manager for the auth's Group model.
           """
       
    1:     use_in_migrations = True
       
    1:     def get_by_natural_key(self, name):
               return self.get(name=name)
       
    1:     async def aget_by_natural_key(self, name):
               return await self.aget(name=name)
       
       
    2: class Group(models.Model):
    1:     """
           Groups are a generic way of categorizing users to apply permissions, or
           some other label, to those users. A user can belong to any number of
           groups.
       
           A user in a group automatically has all the permissions granted to that
           group. For example, if the group 'Site editors' has the permission
           can_edit_home_page, any user in that group will have that permission.
       
           Beyond permissions, groups are a convenient way to categorize users to
           apply some label, or extended functionality, to them. For example, you
           could create a group 'Special users', and you could write code that would
           do special things to those users -- such as giving them access to a
           members-only portion of your site, or sending them members-only email
           messages.
           """
       
    1:     name = models.CharField(_("name"), max_length=150, unique=True)
    2:     permissions = models.ManyToManyField(
    1:         Permission,
    1:         verbose_name=_("permissions"),
    1:         blank=True,
           )
       
    1:     objects = GroupManager()
       
    2:     class Meta:
    1:         verbose_name = _("group")
    1:         verbose_name_plural = _("groups")
       
    1:     def __str__(self):
               return self.name
       
    1:     def natural_key(self):
               return (self.name,)
       
       
    2: class UserManager(BaseUserManager):
    1:     use_in_migrations = True
       
    1:     def _create_user_object(self, username, email, password, **extra_fields):
    3:         if not username:
                   raise ValueError("The given username must be set")
    3:         email = self.normalize_email(email)
               # Lookup the real model class from the global app registry so this
               # manager method can be used in migrations. This is fine because
               # managers are by definition working on the real model.
    6:         GlobalUserModel = apps.get_model(
    3:             self.model._meta.app_label, self.model._meta.object_name
               )
    3:         username = GlobalUserModel.normalize_username(username)
    3:         user = self.model(username=username, email=email, **extra_fields)
    3:         user.password = make_password(password)
    3:         return user
       
    1:     def _create_user(self, username, email, password, **extra_fields):
               """
               Create and save a user with the given username, email, and password.
               """
    3:         user = self._create_user_object(username, email, password, **extra_fields)
    3:         user.save(using=self._db)
    3:         return user
       
    1:     async def _acreate_user(self, username, email, password, **extra_fields):
               """See _create_user()"""
               user = self._create_user_object(username, email, password, **extra_fields)
               await user.asave(using=self._db)
               return user
       
    1:     def create_user(self, username, email=None, password=None, **extra_fields):
    3:         extra_fields.setdefault("is_staff", False)
    3:         extra_fields.setdefault("is_superuser", False)
    3:         return self._create_user(username, email, password, **extra_fields)
       
    1:     create_user.alters_data = True
       
    1:     async def acreate_user(self, username, email=None, password=None, **extra_fields):
               extra_fields.setdefault("is_staff", False)
               extra_fields.setdefault("is_superuser", False)
               return await self._acreate_user(username, email, password, **extra_fields)
       
    1:     acreate_user.alters_data = True
       
    1:     def create_superuser(self, username, email=None, password=None, **extra_fields):
               extra_fields.setdefault("is_staff", True)
               extra_fields.setdefault("is_superuser", True)
       
               if extra_fields.get("is_staff") is not True:
                   raise ValueError("Superuser must have is_staff=True.")
               if extra_fields.get("is_superuser") is not True:
                   raise ValueError("Superuser must have is_superuser=True.")
       
               return self._create_user(username, email, password, **extra_fields)
       
    1:     create_superuser.alters_data = True
       
    1:     async def acreate_superuser(
    1:         self, username, email=None, password=None, **extra_fields
           ):
               extra_fields.setdefault("is_staff", True)
               extra_fields.setdefault("is_superuser", True)
       
               if extra_fields.get("is_staff") is not True:
                   raise ValueError("Superuser must have is_staff=True.")
               if extra_fields.get("is_superuser") is not True:
                   raise ValueError("Superuser must have is_superuser=True.")
       
               return await self._acreate_user(username, email, password, **extra_fields)
       
    1:     acreate_superuser.alters_data = True
       
    1:     def with_perm(
    1:         self, perm, is_active=True, include_superusers=True, backend=None, obj=None
           ):
               if backend is None:
                   backends = auth._get_backends(return_tuples=True)
                   if len(backends) == 1:
                       backend, _ = backends[0]
                   else:
                       raise ValueError(
                           "You have multiple authentication backends configured and "
                           "therefore must provide the `backend` argument."
                       )
               elif not isinstance(backend, str):
                   raise TypeError(
                       "backend must be a dotted import path string (got %r)." % backend
                   )
               else:
                   backend = auth.load_backend(backend)
               if hasattr(backend, "with_perm"):
                   return backend.with_perm(
                       perm,
                       is_active=is_active,
                       include_superusers=include_superusers,
                       obj=obj,
                   )
               return self.none()
       
       
       # A few helper functions for common logic between User and AnonymousUser.
    1: def _user_get_permissions(user, obj, from_name):
           permissions = set()
           name = "get_%s_permissions" % from_name
           for backend in auth.get_backends():
               if hasattr(backend, name):
                   permissions.update(getattr(backend, name)(user, obj))
           return permissions
       
       
    1: async def _auser_get_permissions(user, obj, from_name):
           permissions = set()
           name = "aget_%s_permissions" % from_name
           for backend in auth.get_backends():
               if hasattr(backend, name):
                   permissions.update(await getattr(backend, name)(user, obj))
           return permissions
       
       
    1: def _user_has_perm(user, perm, obj):
           """
           A backend can raise `PermissionDenied` to short-circuit permission checking.
           """
           for backend in auth.get_backends():
               if not hasattr(backend, "has_perm"):
                   continue
               try:
                   if backend.has_perm(user, perm, obj):
                       return True
               except PermissionDenied:
                   return False
           return False
       
       
    1: async def _auser_has_perm(user, perm, obj):
           """See _user_has_perm()"""
           for backend in auth.get_backends():
               if not hasattr(backend, "ahas_perm"):
                   continue
               try:
                   if await backend.ahas_perm(user, perm, obj):
                       return True
               except PermissionDenied:
                   return False
           return False
       
       
    1: def _user_has_module_perms(user, app_label):
           """
           A backend can raise `PermissionDenied` to short-circuit permission checking.
           """
           for backend in auth.get_backends():
               if not hasattr(backend, "has_module_perms"):
                   continue
               try:
                   if backend.has_module_perms(user, app_label):
                       return True
               except PermissionDenied:
                   return False
           return False
       
       
    1: async def _auser_has_module_perms(user, app_label):
           """See _user_has_module_perms()"""
           for backend in auth.get_backends():
               if not hasattr(backend, "ahas_module_perms"):
                   continue
               try:
                   if await backend.ahas_module_perms(user, app_label):
                       return True
               except PermissionDenied:
                   return False
           return False
       
       
    2: class PermissionsMixin(models.Model):
    1:     """
           Add the fields and methods necessary to support the Group and Permission
           models using the ModelBackend.
           """
       
    2:     is_superuser = models.BooleanField(
    1:         _("superuser status"),
    1:         default=False,
    2:         help_text=_(
    1:             "Designates that this user has all permissions without "
                   "explicitly assigning them."
               ),
           )
    2:     groups = models.ManyToManyField(
    1:         Group,
    1:         verbose_name=_("groups"),
    1:         blank=True,
    2:         help_text=_(
    1:             "The groups this user belongs to. A user will get all permissions "
                   "granted to each of their groups."
               ),
    1:         related_name="user_set",
    1:         related_query_name="user",
           )
    2:     user_permissions = models.ManyToManyField(
    1:         Permission,
    1:         verbose_name=_("user permissions"),
    1:         blank=True,
    1:         help_text=_("Specific permissions for this user."),
    1:         related_name="user_set",
    1:         related_query_name="user",
           )
       
    2:     class Meta:
    1:         abstract = True
       
    1:     def get_user_permissions(self, obj=None):
               """
               Return a list of permission strings that this user has directly.
               Query all available auth backends. If an object is passed in,
               return only permissions matching this object.
               """
               return _user_get_permissions(self, obj, "user")
       
    1:     async def aget_user_permissions(self, obj=None):
               """See get_user_permissions()"""
               return await _auser_get_permissions(self, obj, "user")
       
    1:     def get_group_permissions(self, obj=None):
               """
               Return a list of permission strings that this user has through their
               groups. Query all available auth backends. If an object is passed in,
               return only permissions matching this object.
               """
               return _user_get_permissions(self, obj, "group")
       
    1:     async def aget_group_permissions(self, obj=None):
               """See get_group_permissions()"""
               return await _auser_get_permissions(self, obj, "group")
       
    1:     def get_all_permissions(self, obj=None):
               return _user_get_permissions(self, obj, "all")
       
    1:     async def aget_all_permissions(self, obj=None):
               return await _auser_get_permissions(self, obj, "all")
       
    1:     def has_perm(self, perm, obj=None):
               """
               Return True if the user has the specified permission. Query all
               available auth backends, but return immediately if any backend returns
               True. Thus, a user who has permission from a single auth backend is
               assumed to have permission in general. If an object is provided, check
               permissions for that object.
               """
               # Active superusers have all permissions.
               if self.is_active and self.is_superuser:
                   return True
       
               # Otherwise we need to check the backends.
               return _user_has_perm(self, perm, obj)
       
    1:     async def ahas_perm(self, perm, obj=None):
               """See has_perm()"""
               # Active superusers have all permissions.
               if self.is_active and self.is_superuser:
                   return True
       
               # Otherwise we need to check the backends.
               return await _auser_has_perm(self, perm, obj)
       
    1:     def has_perms(self, perm_list, obj=None):
               """
               Return True if the user has each of the specified permissions. If
               object is passed, check if the user has all required perms for it.
               """
               if not isinstance(perm_list, Iterable) or isinstance(perm_list, str):
                   raise ValueError("perm_list must be an iterable of permissions.")
               return all(self.has_perm(perm, obj) for perm in perm_list)
       
    1:     async def ahas_perms(self, perm_list, obj=None):
               """See has_perms()"""
               if not isinstance(perm_list, Iterable) or isinstance(perm_list, str):
                   raise ValueError("perm_list must be an iterable of permissions.")
               for perm in perm_list:
                   if not await self.ahas_perm(perm, obj):
                       return False
               return True
       
    1:     def has_module_perms(self, app_label):
               """
               Return True if the user has any permissions in the given app label.
               Use similar logic as has_perm(), above.
               """
               # Active superusers have all permissions.
               if self.is_active and self.is_superuser:
                   return True
       
               return _user_has_module_perms(self, app_label)
       
    1:     async def ahas_module_perms(self, app_label):
               """See has_module_perms()"""
               # Active superusers have all permissions.
               if self.is_active and self.is_superuser:
                   return True
       
               return await _auser_has_module_perms(self, app_label)
       
       
    2: class AbstractUser(AbstractBaseUser, PermissionsMixin):
    1:     """
           An abstract base class implementing a fully featured User model with
           admin-compliant permissions.
       
           Username and password are required. Other fields are optional.
           """
       
    1:     username_validator = UnicodeUsernameValidator()
       
    2:     username = models.CharField(
    1:         _("username"),
    1:         max_length=150,
    1:         unique=True,
    2:         help_text=_(
    1:             "Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only."
               ),
    1:         validators=[username_validator],
    1:         error_messages={
    1:             "unique": _("A user with that username already exists."),
               },
           )
    1:     first_name = models.CharField(_("first name"), max_length=150, blank=True)
    1:     last_name = models.CharField(_("last name"), max_length=150, blank=True)
    1:     email = models.EmailField(_("email address"), blank=True)
    2:     is_staff = models.BooleanField(
    1:         _("staff status"),
    1:         default=False,
    1:         help_text=_("Designates whether the user can log into this admin site."),
           )
    2:     is_active = models.BooleanField(
    1:         _("active"),
    1:         default=True,
    2:         help_text=_(
    1:             "Designates whether this user should be treated as active. "
                   "Unselect this instead of deleting accounts."
               ),
           )
    1:     date_joined = models.DateTimeField(_("date joined"), default=timezone.now)
       
    1:     objects = UserManager()
       
    1:     EMAIL_FIELD = "email"
    1:     USERNAME_FIELD = "username"
    1:     REQUIRED_FIELDS = ["email"]
       
    2:     class Meta:
    1:         verbose_name = _("user")
    1:         verbose_name_plural = _("users")
    1:         abstract = True
       
    1:     def clean(self):
               super().clean()
               self.email = self.__class__.objects.normalize_email(self.email)
       
    1:     def get_full_name(self):
               """
               Return the first_name plus the last_name, with a space in between.
               """
    2:         full_name = "%s %s" % (self.first_name, self.last_name)
    2:         return full_name.strip()
       
    1:     def get_short_name(self):
               """Return the short name for the user."""
               return self.first_name
       
    1:     def email_user(self, subject, message, from_email=None, **kwargs):
               """Send an email to this user."""
               send_mail(subject, message, from_email, [self.email], **kwargs)
       
       
    2: class User(AbstractUser):
    1:     """
           Users within the Django authentication system are represented by this
           model.
       
           Username and password are required. Other fields are optional.
           """
       
    2:     class Meta(AbstractUser.Meta):
    1:         swappable = "AUTH_USER_MODEL"
       
       
    2: class AnonymousUser:
    1:     id = None
    1:     pk = None
    1:     username = ""
    1:     is_staff = False
    1:     is_active = False
    1:     is_superuser = False
    1:     _groups = EmptyManager(Group)
    1:     _user_permissions = EmptyManager(Permission)
       
    1:     def __str__(self):
               return "AnonymousUser"
       
    1:     def __eq__(self, other):
               return isinstance(other, self.__class__)
       
    1:     def __hash__(self):
               return 1  # instances always return the same hash value
       
    1:     def __int__(self):
               raise TypeError(
                   "Cannot cast AnonymousUser to int. Are you trying to use it in place of "
                   "User?"
               )
       
    1:     def save(self):
               raise NotImplementedError(
                   "Django doesn't provide a DB representation for AnonymousUser."
               )
       
    1:     def delete(self):
               raise NotImplementedError(
                   "Django doesn't provide a DB representation for AnonymousUser."
               )
       
    1:     def set_password(self, raw_password):
               raise NotImplementedError(
                   "Django doesn't provide a DB representation for AnonymousUser."
               )
       
    1:     def check_password(self, raw_password):
               raise NotImplementedError(
                   "Django doesn't provide a DB representation for AnonymousUser."
               )
       
    2:     @property
    2:     def groups(self):
               return self._groups
       
    2:     @property
    2:     def user_permissions(self):
               return self._user_permissions
       
    1:     def get_user_permissions(self, obj=None):
               return _user_get_permissions(self, obj, "user")
       
    1:     async def aget_user_permissions(self, obj=None):
               return await _auser_get_permissions(self, obj, "user")
       
    1:     def get_group_permissions(self, obj=None):
               return set()
       
    1:     async def aget_group_permissions(self, obj=None):
               return self.get_group_permissions(obj)
       
    1:     def get_all_permissions(self, obj=None):
               return _user_get_permissions(self, obj, "all")
       
    1:     async def aget_all_permissions(self, obj=None):
               return await _auser_get_permissions(self, obj, "all")
       
    1:     def has_perm(self, perm, obj=None):
               return _user_has_perm(self, perm, obj=obj)
       
    1:     async def ahas_perm(self, perm, obj=None):
               return await _auser_has_perm(self, perm, obj=obj)
       
    1:     def has_perms(self, perm_list, obj=None):
               if not isinstance(perm_list, Iterable) or isinstance(perm_list, str):
                   raise ValueError("perm_list must be an iterable of permissions.")
               return all(self.has_perm(perm, obj) for perm in perm_list)
       
    1:     async def ahas_perms(self, perm_list, obj=None):
               if not isinstance(perm_list, Iterable) or isinstance(perm_list, str):
                   raise ValueError("perm_list must be an iterable of permissions.")
               for perm in perm_list:
                   if not await self.ahas_perm(perm, obj):
                       return False
               return True
       
    1:     def has_module_perms(self, module):
               return _user_has_module_perms(self, module)
       
    1:     async def ahas_module_perms(self, module):
               return await _auser_has_module_perms(self, module)
       
    2:     @property
    2:     def is_anonymous(self):
               return True
       
    2:     @property
    2:     def is_authenticated(self):
   23:         return False
       
    1:     def get_username(self):
               return self.username
