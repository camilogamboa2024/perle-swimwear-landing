    1: """
       Functions for reversing a regular expression (used in reverse URL resolving).
       Used internally by Django and not intended for external use.
       
       This is not, and is not intended to be, a complete reg-exp decompiler. It
       should be good enough for a large class of URLS, however.
       """
       
    1: import re
       
    1: from django.utils.functional import SimpleLazyObject
       
       # Mapping of an escape character to a representative of that class. So, e.g.,
       # "\w" is replaced by "x" in a reverse URL. A value of None means to ignore
       # this sequence. Any missing key is mapped to itself.
    1: ESCAPE_MAPPINGS = {
    1:     "A": None,
    1:     "b": None,
    1:     "B": None,
    1:     "d": "0",
    1:     "D": "x",
    1:     "s": " ",
    1:     "S": "x",
    1:     "w": "x",
    1:     "W": "!",
    1:     "Z": None,
       }
       
       
    2: class Choice(list):
    1:     """Represent multiple possibilities at this point in a pattern string."""
       
       
    2: class Group(list):
    1:     """Represent a capturing group in the pattern string."""
       
       
    2: class NonCapture(list):
    1:     """Represent a non-capturing group in the pattern string."""
       
       
    1: def normalize(pattern):
           r"""
           Given a reg-exp pattern, normalize it to an iterable of forms that
           suffice for reverse matching. This does the following:
       
           (1) For any repeating sections, keeps the minimum number of occurrences
               permitted (this means zero for optional groups).
           (2) If an optional group includes parameters, include one occurrence of
               that group (along with the zero occurrence case from step (1)).
           (3) Select the first (essentially an arbitrary) element from any character
               class. Select an arbitrary character for any unordered class (e.g. '.'
               or '\w') in the pattern.
           (4) Ignore look-ahead and look-behind assertions.
           (5) Raise an error on any disjunctive ('|') constructs.
       
           Django's URLs for forward resolving are either all positional arguments or
           all keyword arguments. That is assumed here, as well. Although reverse
           resolving can be done using positional args when keyword args are
           specified, the two cannot be mixed in the same reverse() call.
           """
           # Do a linear scan to work out the special features of this pattern. The
           # idea is that we scan once here and collect all the information we need to
           # make future decisions.
  327:     result = []
  327:     non_capturing_groups = []
  327:     consume_next = True
  327:     pattern_iter = next_char(iter(pattern))
  327:     num_args = 0
       
           # A "while" loop is used here because later on we need to be able to peek
           # at the next character and possibly go around without consuming another
           # one at the top of the loop.
  327:     try:
  327:         ch, escaped = next(pattern_iter)
    2:     except StopIteration:
    2:         return [("", [])]
       
  325:     try:
 5514:         while True:
 5514:             if escaped:
    1:                 result.append(ch)
 5513:             elif ch == ".":
                       # Replace "any character" with an arbitrary representative.
                       result.append(".")
 5513:             elif ch == "|":
                       # FIXME: One day we'll should do this, but not in 1.0.
                       raise NotImplementedError("Awaiting Implementation")
 5513:             elif ch == "^":
  122:                 pass
 5391:             elif ch == "$":
    2:                 break
 5389:             elif ch == ")":
                       # This can only be the end of a non-capturing group, since all
                       # other unescaped parentheses are handled by the grouping
                       # section later (and the full group is handled there).
                       #
                       # We regroup everything inside the capturing group so that it
                       # can be quantified, if necessary.
                       start = non_capturing_groups.pop()
                       inner = NonCapture(result[start:])
                       result = result[:start] + [inner]
 5389:             elif ch == "[":
                       # Replace ranges with the first character in the range.
                       ch, escaped = next(pattern_iter)
                       result.append(ch)
                       ch, escaped = next(pattern_iter)
                       while escaped or ch != "]":
                           ch, escaped = next(pattern_iter)
 5389:             elif ch == "(":
                       # Some kind of group.
  195:                 ch, escaped = next(pattern_iter)
  195:                 if ch != "?" or escaped:
                           # A positional group
                           name = "_%d" % num_args
                           num_args += 1
                           result.append(Group((("%%(%s)s" % name), name)))
                           walk_to_end(ch, pattern_iter)
                       else:
  195:                     ch, escaped = next(pattern_iter)
  195:                     if ch in "!=<":
                               # All of these are ignorable. Walk to the end of the
                               # group.
                               walk_to_end(ch, pattern_iter)
  195:                     elif ch == ":":
                               # Non-capturing group
                               non_capturing_groups.append(len(result))
  195:                     elif ch != "P":
                               # Anything else, other than a named group, is something
                               # we cannot reverse.
                               raise ValueError("Non-reversible reg-exp portion: '(?%s'" % ch)
                           else:
  195:                         ch, escaped = next(pattern_iter)
  195:                         if ch not in ("<", "="):
                                   raise ValueError(
                                       "Non-reversible reg-exp portion: '(?P%s'" % ch
                                   )
                               # We are in a named capturing group. Extra the name and
                               # then skip to the end.
  195:                         if ch == "<":
  195:                             terminal_char = ">"
                               # We are in a named backreference.
                               else:
                                   terminal_char = ")"
  195:                         name = []
  195:                         ch, escaped = next(pattern_iter)
 1893:                         while ch != terminal_char:
 1698:                             name.append(ch)
 1698:                             ch, escaped = next(pattern_iter)
  195:                         param = "".join(name)
                               # Named backreferences have already consumed the
                               # parenthesis.
  195:                         if terminal_char != ")":
  195:                             result.append(Group((("%%(%s)s" % param), param)))
  195:                             walk_to_end(ch, pattern_iter)
                               else:
                                   result.append(Group((("%%(%s)s" % param), None)))
 5194:             elif ch in "*?+{":
                       # Quantifiers affect the previous item in the result list.
                       count, ch = get_quantifier(ch, pattern_iter)
                       if ch:
                           # We had to look ahead, but it wasn't need to compute the
                           # quantifier, so use this character next time around the
                           # main loop.
                           consume_next = False
       
                       if count == 0:
                           if contains(result[-1], Group):
                               # If we are quantifying a capturing group (or
                               # something containing such a group) and the minimum is
                               # zero, we must also handle the case of one occurrence
                               # being present. All the quantifiers (except {0,0},
                               # which we conveniently ignore) that have a 0 minimum
                               # also allow a single occurrence.
                               result[-1] = Choice([None, result[-1]])
                           else:
                               result.pop()
                       elif count > 1:
                           result.extend([result[-1]] * (count - 1))
                   else:
                       # Anything else is a literal.
 5194:                 result.append(ch)
       
 5512:             if consume_next:
 5512:                 ch, escaped = next(pattern_iter)
 5189:             consume_next = True
  323:     except StopIteration:
  323:         pass
           except NotImplementedError:
               # A case of using the disjunctive form. No results for you!
               return [("", [])]
       
  325:     return list(zip(*flatten_result(result)))
       
       
    1: def next_char(input_iter):
           r"""
           An iterator that yields the next character from "pattern_iter", respecting
           escape sequences. An escaped character is replaced by a representative of
           its class (e.g. \w -> "x"). If the escaped character is one that is
           skipped, it is not returned (the next character is returned instead).
       
           Yield the next character, along with a boolean indicating whether it is a
           raw (unescaped) character or not.
           """
 9531:     for ch in input_iter:
 9206:         if ch != "\\":
 8880:             yield ch, False
 8878:             continue
  326:         ch = next(input_iter)
  326:         representative = ESCAPE_MAPPINGS.get(ch, ch)
  326:         if representative is None:
  325:             continue
    1:         yield representative, True
       
       
    1: def walk_to_end(ch, input_iter):
           """
           The iterator is currently inside a capturing group. Walk to the close of
           this group, skipping over any nested groups and handling escaped
           parentheses correctly.
           """
  195:     if ch == "(":
               nesting = 1
           else:
  195:         nesting = 0
  889:     for ch, escaped in input_iter:
  889:         if escaped:
                   continue
  889:         elif ch == "(":
                   nesting += 1
  889:         elif ch == ")":
  195:             if not nesting:
  195:                 return
                   nesting -= 1
       
       
    1: def get_quantifier(ch, input_iter):
           """
           Parse a quantifier from the input, where "ch" is the first character in the
           quantifier.
       
           Return the minimum number of occurrences permitted by the quantifier and
           either None or the next character from the input_iter if the next character
           is not part of the quantifier.
           """
           if ch in "*?+":
               try:
                   ch2, escaped = next(input_iter)
               except StopIteration:
                   ch2 = None
               if ch2 == "?":
                   ch2 = None
               if ch == "+":
                   return 1, ch2
               return 0, ch2
       
           quant = []
           while ch != "}":
               ch, escaped = next(input_iter)
               quant.append(ch)
           quant = quant[:-1]
           values = "".join(quant).split(",")
       
           # Consume the trailing '?', if necessary.
           try:
               ch, escaped = next(input_iter)
           except StopIteration:
               ch = None
           if ch == "?":
               ch = None
           return int(values[0]), ch
       
       
    1: def contains(source, inst):
           """
           Return True if the "source" contains an instance of "inst". False,
           otherwise.
           """
           if isinstance(source, inst):
               return True
           if isinstance(source, NonCapture):
               for elt in source:
                   if contains(elt, inst):
                       return True
           return False
       
       
    1: def flatten_result(source):
           """
           Turn the given source sequence into a list of reg-exp possibilities and
           their arguments. Return a list of strings and a list of argument lists.
           Each of the two lists will be of the same length.
           """
  325:     if source is None:
               return [""], [[]]
  325:     if isinstance(source, Group):
               if source[1] is None:
                   params = []
               else:
                   params = [source[1]]
               return [source[0]], [params]
  325:     result = [""]
  325:     result_args = [[]]
  325:     pos = last = 0
 5715:     for pos, elt in enumerate(source):
 5390:         if isinstance(elt, str):
 5195:             continue
  195:         piece = "".join(source[last:pos])
  195:         if isinstance(elt, Group):
  195:             piece += elt[0]
  195:             param = elt[1]
               else:
                   param = None
  195:         last = pos + 1
  390:         for i in range(len(result)):
  195:             result[i] += piece
  195:             if param:
  195:                 result_args[i].append(param)
  195:         if isinstance(elt, (Choice, NonCapture)):
                   if isinstance(elt, NonCapture):
                       elt = [elt]
                   inner_result, inner_args = [], []
                   for item in elt:
                       res, args = flatten_result(item)
                       inner_result.extend(res)
                       inner_args.extend(args)
                   new_result = []
                   new_args = []
                   for item, args in zip(result, result_args):
                       for i_item, i_args in zip(inner_result, inner_args):
                           new_result.append(item + i_item)
                           new_args.append(args[:] + i_args)
                   result = new_result
                   result_args = new_args
  325:     if pos >= last:
  324:         piece = "".join(source[last:])
  648:         for i in range(len(result)):
  324:             result[i] += piece
  325:     return result, result_args
       
       
    1: def _lazy_re_compile(regex, flags=0):
           """Lazily compile a regex with flags."""
       
   83:     def _compile():
               # Compile the regex if it was not passed pre-compiled.
   19:         if isinstance(regex, (str, bytes)):
   19:             return re.compile(regex, flags)
               else:
                   assert not flags, "flags must be empty if regex is passed pre-compiled"
                   return regex
       
   83:     return SimpleLazyObject(_compile)
