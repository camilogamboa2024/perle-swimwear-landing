    1: from django.contrib.messages.api import get_messages
    1: from django.contrib.messages.constants import DEFAULT_LEVELS
       
       
    1: def messages(request):
           """
           Return a lazy 'messages' context variable as well as
           'DEFAULT_MESSAGE_LEVELS'.
           """
   20:     return {
   20:         "messages": get_messages(request),
   20:         "DEFAULT_MESSAGE_LEVELS": DEFAULT_LEVELS,
           }
