    1: """Generic (shallow and deep) copying operations.
       
       Interface summary:
       
               import copy
       
               x = copy.copy(y)        # make a shallow copy of y
               x = copy.deepcopy(y)    # make a deep copy of y
       
       For module specific errors, copy.Error is raised.
       
       The difference between shallow and deep copying is only relevant for
       compound objects (objects that contain other objects, like lists or
       class instances).
       
       - A shallow copy constructs a new compound object and then (to the
         extent possible) inserts *the same objects* into it that the
         original contains.
       
       - A deep copy constructs a new compound object and then, recursively,
         inserts *copies* into it of the objects found in the original.
       
       Two problems often exist with deep copy operations that don't exist
       with shallow copy operations:
       
        a) recursive objects (compound objects that, directly or indirectly,
           contain a reference to themselves) may cause a recursive loop
       
        b) because deep copy copies *everything* it may copy too much, e.g.
           administrative data structures that should be shared even between
           copies
       
       Python's deep copy operation avoids these problems by:
       
        a) keeping a table of objects already copied during the current
           copying pass
       
        b) letting user-defined classes override the copying operation or the
           set of components copied
       
       This version does not copy types like module, class, function, method,
       nor stack trace, stack frame, nor file, socket, window, nor any
       similar types.
       
       Classes can use the same interfaces to control copying that they use
       to control pickling: they can define methods called __getinitargs__(),
       __getstate__() and __setstate__().  See the documentation for module
       "pickle" for information on these methods.
       """
       
    1: import types
    1: import weakref
    1: from copyreg import dispatch_table
       
    2: class Error(Exception):
    1:     pass
    1: error = Error   # backward compatibility
       
    1: try:
    1:     from org.python.core import PyStringMap
    1: except ImportError:
    1:     PyStringMap = None
       
    1: __all__ = ["Error", "copy", "deepcopy"]
       
    1: def copy(x):
           """Shallow copy operation on arbitrary Python objects.
       
           See the module's __doc__ string for more info.
           """
       
 1093:     cls = type(x)
       
 1093:     copier = _copy_dispatch.get(cls)
 1093:     if copier:
   94:         return copier(x)
       
  999:     if issubclass(cls, type):
               # treat it as a regular class:
               return _copy_immutable(x)
       
  999:     copier = getattr(cls, "__copy__", None)
  999:     if copier is not None:
  246:         return copier(x)
       
  753:     reductor = dispatch_table.get(cls)
  753:     if reductor is not None:
               rv = reductor(x)
           else:
  753:         reductor = getattr(x, "__reduce_ex__", None)
  753:         if reductor is not None:
  753:             rv = reductor(4)
               else:
                   reductor = getattr(x, "__reduce__", None)
                   if reductor:
                       rv = reductor()
                   else:
                       raise Error("un(shallow)copyable object of type %s" % cls)
       
  753:     if isinstance(rv, str):
               return x
  753:     return _reconstruct(x, None, *rv)
       
       
    1: _copy_dispatch = d = {}
       
    1: def _copy_immutable(x):
           return x
   20: for t in (type(None), int, float, bool, complex, str, tuple,
    1:           bytes, frozenset, type, range, slice, property,
    1:           types.BuiltinFunctionType, type(Ellipsis), type(NotImplemented),
    1:           types.FunctionType, weakref.ref):
   18:     d[t] = _copy_immutable
    1: t = getattr(types, "CodeType", None)
    1: if t is not None:
    1:     d[t] = _copy_immutable
       
    1: d[list] = list.copy
    1: d[dict] = dict.copy
    1: d[set] = set.copy
    1: d[bytearray] = bytearray.copy
       
    1: if PyStringMap is not None:
           d[PyStringMap] = PyStringMap.copy
       
    1: del d, t
       
    1: def deepcopy(x, memo=None, _nil=[]):
           """Deep copy operation on arbitrary Python objects.
       
           See the module's __doc__ string for more info.
           """
       
 5136:     if memo is None:
  374:         memo = {}
       
 5136:     d = id(x)
 5136:     y = memo.get(d, _nil)
 5136:     if y is not _nil:
               return y
       
 5136:     cls = type(x)
       
 5136:     copier = _deepcopy_dispatch.get(cls)
 5136:     if copier is not None:
 3994:         y = copier(x, memo)
           else:
 1142:         if issubclass(cls, type):
  670:             y = _deepcopy_atomic(x, memo)
               else:
  472:             copier = getattr(x, "__deepcopy__", None)
  472:             if copier is not None:
  382:                 y = copier(memo)
                   else:
   90:                 reductor = dispatch_table.get(cls)
   90:                 if reductor:
                           rv = reductor(x)
                       else:
   90:                     reductor = getattr(x, "__reduce_ex__", None)
   90:                     if reductor is not None:
   90:                         rv = reductor(4)
                           else:
                               reductor = getattr(x, "__reduce__", None)
                               if reductor:
                                   rv = reductor()
                               else:
                                   raise Error(
                                       "un(deep)copyable object of type %s" % cls)
   90:                 if isinstance(rv, str):
                           y = x
                       else:
   90:                     y = _reconstruct(x, memo, *rv)
       
           # If is its own copy, don't memoize.
 5136:     if y is not x:
 1438:         memo[d] = y
 1438:         _keep_alive(x, memo) # Make sure x lives at least as long as d
 5136:     return y
       
    1: _deepcopy_dispatch = d = {}
       
    1: def _deepcopy_atomic(x, memo):
 3450:     return x
    1: d[type(None)] = _deepcopy_atomic
    1: d[type(Ellipsis)] = _deepcopy_atomic
    1: d[type(NotImplemented)] = _deepcopy_atomic
    1: d[int] = _deepcopy_atomic
    1: d[float] = _deepcopy_atomic
    1: d[bool] = _deepcopy_atomic
    1: d[complex] = _deepcopy_atomic
    1: d[bytes] = _deepcopy_atomic
    1: d[str] = _deepcopy_atomic
    1: d[types.CodeType] = _deepcopy_atomic
    1: d[type] = _deepcopy_atomic
    1: d[range] = _deepcopy_atomic
    1: d[types.BuiltinFunctionType] = _deepcopy_atomic
    1: d[types.FunctionType] = _deepcopy_atomic
    1: d[weakref.ref] = _deepcopy_atomic
    1: d[property] = _deepcopy_atomic
       
    1: def _deepcopy_list(x, memo, deepcopy=deepcopy):
   99:     y = []
   99:     memo[id(x)] = y
   99:     append = y.append
  317:     for a in x:
  218:         append(deepcopy(a, memo))
   99:     return y
    1: d[list] = _deepcopy_list
       
    1: def _deepcopy_tuple(x, memo, deepcopy=deepcopy):
  694:     y = [deepcopy(a, memo) for a in x]
           # We're not going to put the tuple in the memo, but it's still important we
           # check for it, in case the tuple contains recursive mutable structures.
  248:     try:
  248:         return memo[id(x)]
  248:     except KeyError:
  248:         pass
  446:     for k, j in zip(x, y):
  198:         if k is not j:
                   y = tuple(y)
                   break
           else:
  248:         y = x
  248:     return y
    1: d[tuple] = _deepcopy_tuple
       
    1: def _deepcopy_dict(x, memo, deepcopy=deepcopy):
  867:     y = {}
  867:     memo[id(x)] = y
 2528:     for key, value in x.items():
 1661:         y[deepcopy(key, memo)] = deepcopy(value, memo)
  867:     return y
    1: d[dict] = _deepcopy_dict
    1: if PyStringMap is not None:
           d[PyStringMap] = _deepcopy_dict
       
    1: def _deepcopy_method(x, memo): # Copy instance methods
           return type(x)(x.__func__, deepcopy(x.__self__, memo))
    1: d[types.MethodType] = _deepcopy_method
       
    1: del d
       
    1: def _keep_alive(x, memo):
           """Keeps a reference to the object x in the memo.
       
           Because we remember objects by their id, we have
           to assure that possibly temporary objects are kept
           alive by referencing them.
           We store a reference at the id of the memo, which should
           normally not be used unless someone tries to deepcopy
           the memo itself...
           """
 1438:     try:
 1438:         memo[id(memo)].append(x)
  336:     except KeyError:
               # aha, this is the first one :-)
  336:         memo[id(memo)]=[x]
       
    2: def _reconstruct(x, memo, func, args,
    1:                  state=None, listiter=None, dictiter=None,
    1:                  *, deepcopy=deepcopy):
  843:     deep = memo is not None
  843:     if deep and args:
  270:         args = (deepcopy(arg, memo) for arg in args)
  843:     y = func(*args)
  843:     if deep:
   90:         memo[id(x)] = y
       
  843:     if state is not None:
  800:         if deep:
   47:             state = deepcopy(state, memo)
  800:         if hasattr(y, '__setstate__'):
                   y.__setstate__(state)
               else:
  800:             if isinstance(state, tuple) and len(state) == 2:
                       state, slotstate = state
                   else:
  800:                 slotstate = None
  800:             if state is not None:
  800:                 y.__dict__.update(state)
  800:             if slotstate is not None:
                       for key, value in slotstate.items():
                           setattr(y, key, value)
       
  843:     if listiter is not None:
               if deep:
                   for item in listiter:
                       item = deepcopy(item, memo)
                       y.append(item)
               else:
                   for item in listiter:
                       y.append(item)
  843:     if dictiter is not None:
   43:         if deep:
  338:             for key, value in dictiter:
  295:                 key = deepcopy(key, memo)
  295:                 value = deepcopy(value, memo)
  295:                 y[key] = value
               else:
                   for key, value in dictiter:
                       y[key] = value
  843:     return y
       
    1: del types, weakref, PyStringMap
